# Chat-Work-Agent 产品需求文档 (PRD)

| 版本 | 日期 | 作者 | 修订说明 |
|------|------|------|----------|
| v1.0 | 2026-09-15 | — | 初稿 |
| v1.1 | 2026-09-15 | — | 补充 Agent Core 架构、MVP 具体表单、评估方法论、成本估算 |
| v1.2 | 2026-09-15 | — | 审核修订：MCP 规范合规（命名/annotations）、幂等设计防重复录单、默认值规则、成本与时间线口径对齐；新增风险应对、记忆写入机制、PIPL 知情同意、数据分级、区域权限、术语表 |
| v1.3 | 2026-09-15 | — | 补充 SSO 认证实现细节（8.5）：Identity Broker 架构、OIDC+PKCE 时序、免登流程、JWT Claims 设计、账号映射与生命周期联动、会话安全策略 |
| v1.4 | 2026-09-15 | — | 新增技能体系设计（第 3 章重写，参考 WorkBuddy SkillHub）：技能包结构、三种执行模式（Ask/Plan/Craft）、技能分类与市场、生命周期、触发机制、MVP 官方技能清单；Agent Core 技能优先路由；Phase 2/3 技能相关能力对齐 |
| v1.5 | 2026-09-15 | — | 补充自动化任务设计（3.6 扩展，参考 WorkBuddy 自动化）：任务配置结构、调度规则、执行约束防护栏、执行身份权限、先跑通再自动化流程、模板库、管理界面；新增知识库设计（9.5，参考 WorkBuddy 项目资产库/乐享）：知识空间分级、入库流程、RAG 注入机制、管理功能；Phase 2/3、里程碑、待确认清单、术语表联动更新 |
| v1.6 | 2026-09-15 | — | 新增桌面端客户端设计（6.4，Electron）：Web 之外的增量价值、技术选型（Electron 主/Tauri 备）、进程架构与 6 条安全红线、本地 MCP Server（local__ 文件工具）、分发与灰度更新、Quick Ask 小窗；新增 8.5.8 桌面端登录流程（系统浏览器 + Loopback，RFC 8252）；IdP 矩阵、技术选型、里程碑、待确认（信创优先级/签名证书）、风险 R11-R12、术语表联动更新 |
| v1.6.1 | 2026-09-15 | — | 新增高保真交互原型 prototype.html（三栏布局、订单录入 HITL 确认卡、审批二次确认、BI 只读查询、模式切换、记忆面板）；PRD 新增 5.4 原型覆盖对照表；新增技术架构规划 ARCHITECTURE.md（Python/TS/Electron，Monorepo 工程结构与模块设计），第 12 章引用 |
| v1.7 | 2026-09-15 | — | **客户端形态决策变更：取消独立 Web 端，Electron 桌面端为 MVP 唯一客户端**（6.4 迁移为 5.5，交付期从 Phase 2 提前至 MVP）；登录与组织基础功能新增 A-06 桌面端客户端；8.5.1 IdP 矩阵精简（Web/企微/钉钉网页端行移除，小程序归 Phase 3）；8.5.2 改为通用 OIDC 授权原理；8.5.3 标注 Phase 3；技术选型/里程碑/风险 R11-R12/待确认/成本/术语表联动更新。原型完善六大视图：SSO 登录页（AD/企微/钉钉三渠道，对应 8.5）、技能市场（对应 3.4）、知识库（对应 9.5）、自动化任务（对应 3.6）、管理我的记忆（对应 9.1-9.3）、设置（对应 5.1 A-03）；5.4 原型覆盖表同步更新 |
| v1.7.1 | 2026-09-15 | — | 覆盖率目标纳入 CRM（集团销售下单/跟单系统）：OA + BI + CRM 三大核心系统；6.1 CRM 优先场景改为销售下单/跟单，术语表说明同步更新；修复两机并行编辑冲突（版本记录去重、5.5 节残留表格清理、6.1 CRM 行恢复） |
| v1.7.2 | 2026-09-16 | — | 原型新增三处交互演示态：ERP 复杂单据录入（物料歧义候选选择卡、单据摘要卡、diff 变更卡、全屏单据工作台三级表主/子/孙 + 仅看异常行 + 批次懒加载钻取、提交成功流水线）、技能市场上架技能三步向导（基本信息/工具与数据范围声明（只读/写入徽标）→ 信息科安全评审（按工具读写动态评审规则）→ 发布上架，市场「评审中」卡片与统计联动）、自动化任务模板库弹层与新建任务四步向导（来源三选：会话转换/模板创建/空白新建 → 调度与推送配置（防护栏）→ 试运行 HITL 确认 → 启用插卡）；5.4 原型覆盖表同步更新（技能市场行、自动化任务行扩展，新增 ERP 复杂单据行） |
| v1.7.3 | 2026-09-16 | — | 业务定位纠正：OA 侧 MVP 聚焦**流程审批**（场景 1-1 改为请假审批 `oa_leave_request`，新增场景 1-3 报销审批 `oa_expense_report`，请购单/待办审批保留，技能清单 5→6 个）；销售订单录入（含提交审批）整体迁入 Phase 2 CRM（新增 6.1.1，技能 `crm_sales_order_entry`，客户主数据/成交价改从 CRM 带出）；收窄原则、3.2 技能示例、5.4 覆盖表、6.3 跨系统编排、8 章工具示例、16 章评测集联动更新 |
| v1.7.4 | 2026-09-16 | — | 8.2 追加遗留系统读写分离规则摘要（详见 ARCHITECTURE 4.2.1）：老系统无需"支持 MCP"（MCP Server 为我方适配进程）；L1~L5 能力分级（OpenAPI/WebService/仅数据库/纯 C-S 界面/仅 Excel）对应读路径与写路径选择；读写分离铁律（禁 DB 直写业务表、RPA 写入兜底、文件导入暂存区对账）；降级链 `API → RPA → 文件导入 → 人工兜底` |
| v1.7.5 | 2026-09-17 | — | 关系库选型变更：**MySQL 8.0+ 优先**（原 PostgreSQL 降为兼容备选）。技术选型表关系库行调整（详见 ARCHITECTURE 4.6 双库兼容原则）、成本估算数据库服务器与向量库描述联动更新 |
| v1.7.6 | 2026-09-17 | — | 新增 **5.6 管理端设计**（分阶段形态：MVP 桌面端角色菜单 → Phase 2 独立 Web 管理后台 admin.chat-work.internal）：5.6.1 管理角色矩阵（系统管理员/安全评审员/知识库管理员/组织管理员/审计员）、5.6.2 六大工作台清单与阶段（审计日志/权限版本 MVP 起，技能评审/知识库审核/自动化治理/系统配置 Phase 2）、5.6.3 复用架构（渲染层/ui 包/api-client 增量成本控制）、5.6.4 管理操作自身约束（审计者也被审计/双人复核/会话锁定）；里程碑 Phase 2 增 Web 管理后台；5.4 原型覆盖表新增管理后台视图行；原型新增管理后台视图（评审工作台/知识库审核/审计日志/用户与权限四个 Tab，侧栏管理员入口） |
| v1.7.7 | 2026-09-17 | — | MVP 二次收窄（OA 侧只留请假审批）：删除场景 1-2 请购单录入、场景 1-3 报销审批（含技能清单 oa_purchase_request/oa_expense_report，官方技能 6→4 个）；新增 6.1.2 OA 表单扩展承接（报销/请购迁入 Phase 2，表单定义与交互模式复用场景 1-1）；收窄原则、5.2 标题、5.4 覆盖表、6.1.1 业务澄清、16.1 评测集（报销/请购标注 Phase 2）联动更新 |
| v1.8 | 2026-09-18 | — | 第 13 章新增 13.1 MVP 开发进度状态表：Agent Core 流水线、4 个官方技能（请假录入 oa_leave_request / 待办审批 oa_todo_approve / BI 查询 bi_query / 周报生成 weekly_report）与桌面端（Electron）接入均标记 ✅ 已完成（单测 40 passed + 桌面端 E2E 通过）；≥3 天审批流含部门总监节点、审批"这条"承接定位与驳回原因提取（PRD 场景 2 核心验收句全链路）标记 ✅ 已验证；SSO 登录 / 组织权限实接 / 审计日志 / 生产部署与灰度标记 📋 待开发并注明前置依赖 |

---

## 1. 背景与目标

### 1.1 背景

集团公司内部存在多个业务系统（OA、BI、CRM、ERP、WMS、MES、U8 等），员工日常工作需要在各系统间频繁切换，重复录入数据、手动统计报表、跨系统查询效率低下。各科室之间信息流转依赖线下沟通或零散的消息工具，知识沉淀和经验复用不足。

### 1.2 产品定位

**Chat-Work-Agent** 是一款面向集团内部员工的 AI 工作助手，以自然语言对话为交互方式，打通各业务系统的数据与操作，帮助员工完成单据录入、审批、查询统计、报告生成等日常工作，并逐步构建**个人记忆**与**组织记忆**。

### 1.3 目标

| 目标维度 | 指标 |
|----------|------|
| 效率提升 | MVP 阶段目标 60% 高频查询类工作可通过对话完成 |
| 覆盖率 | 上线 6 个月内覆盖 OA + BI + CRM 三大核心系统，对接 4+ 科室 |
| 准确性 | 单据录入准确率 ≥ 99%，查询结果与源系统一致率 100% |
| 用户体验 | NPS ≥ 40，日活员工占比 ≥ 30% |

### 1.4 非目标（首期不做）

- 替代现有业务系统，仅作为**统一交互入口**
- 全自动无人值守审批（关键动作必须人工确认）
- 面向外部客户使用

---

## 2. 用户角色与权限模型

### 2.1 组织架构

```
集团公司
├── 事业部 A
│   ├── 销售科
│   ├── 财务科
│   ├── 信息科
│   ├── ...
│   └── 仓库物流
├── 事业部 B
│   └── ...
└── 总部
    ├── 人力资源科
    ├── 企管科
    ├── 法务科
    └── ...
```

### 2.2 角色定义

| 角色 | 说明 | Agent 能力范围 |
|------|------|----------------|
| 普通员工 | 各科室一线操作 | 本人单据录入/查询、本人待办、个人统计 |
| 科室主管 | 科室负责人 | 本科室全部数据查询、本科室单据审核、团队统计 |
| 部门经理 | 部门/事业部负责人 | 本部门数据、审批流管理、多维统计 |
| 系统管理员 | 信息科专人 | Agent 配置、权限管理、系统集成维护 |
| 审计/合规 | 法务/品管专人 | 只读全量审计日志、敏感操作追溯 |

### 2.3 权限矩阵（核心原则）

1. **系统级权限**：员工是否有权访问 OA / BI / CRM 等由 HR 主数据同步
2. **数据级权限——组织维度**：本人 → 本科室 → 本部门 → 集团，逐级向上
3. **数据级权限——业务维度**：区域（华东/华南/…）、产品线、客户归属，与组织维度**独立校验**（如"华东区销售额"要求用户对华东区有数据权限，仅组织权限不够）
4. **操作级权限**：查询 < 录入 < 提交 < 审核 < 审批，逐级严格
5. **跨系统权限**：员工在 A 系统的权限不自动等价于 B 系统，需各自映射

> Agent 的每一个动作都必须同时满足以上权限维度校验（2/3 为"与"关系，任一不满足即拒绝），缺一不可。

---

## 3. 功能架构与技能体系

### 3.1 功能架构总览

```
┌──────────────────────────────────────────────────────────────┐
│                       Chat-Work-Agent                          │
├─────────┬──────────┬────────────────┬──────────┬─────────────┤
│  对话层  │  能力层   │ 记忆与知识层    │ 集成层(MCP)│   安全层    │
│ Chat UI │ Skills + │ Memory + KB    │ Gateway  │  Guardrail  │
│         │ 自动化    │ (RAG 注入)     │          │             │
├─────────┴──────────┴────────────────┴──────────┴─────────────┤
│            OA │ BI │ CRM │ ERP │ WMS │ MES │ U8  …           │
│            知识源：制度库 / SOP / FAQ（文档入库，见 9.5）       │
└──────────────────────────────────────────────────────────────┘
```

### 3.2 技能（Skill）体系设计

> 参考腾讯 WorkBuddy 的 SkillHub 模式：技能是面向业务场景的**能力扩展包**，把"意图识别 → 工具编排 → 表单交互 → 确认 → 执行 → 交付"封装为**可复用、可分享、可上架**的独立单元。

#### 3.2.1 技能是什么：工具与技能的两层关系

| 层 | 定位 | 类比 | 示例 |
|----|------|------|------|
| **MCP 工具** | 原子操作（单个系统、单次调用） | 动词 | `oa__submit_leave_request`、`oa__query_leave_balance` |
| **技能** | 业务流程编排（跨步骤、可含表单与确认） | 用例 | "请假审批" = 查余额 → 抽取起止/事由 → 时长自动计算 → 表单确认 → 提交 → 返回审批单号 |

- 一个技能可编排**多个工具**（跨系统）；一个工具可被**多个技能**复用
- 技能是用户可感知的最小能力单元（用户说"帮我录单"命中的是技能，不是工具）
- 技能是权限挂载的推荐单元（安装技能 = 授予其声明的工具权限子集）

#### 3.2.2 技能包（Skill Package）结构

```jsonc
{
  "skillId": "oa_leave_request",
  "name": "请假审批",
  "version": "1.2.0",
  "owner": "信息科",              // 官方 | 科室 | 个人
  "scope": ["全员"],              // 适用范围（安装范围）
  "trigger": {
    "utterances": ["我要请假", "请两天年假", "帮我提交请假单"],  // 触发问法（供意图识别）
    "schedule": null,             // 定时触发（cron，Phase 2）
    "events": null                // 事件触发（订阅系统事件，Phase 2）
  },
  "mode": "craft",                // 默认执行模式：ask | plan | craft（见 3.3）
  "flow": [                       // 步骤编排（DAG）
    { "step": 1, "tool": "oa__query_leave_balance",   "desc": "查年假/调休余额（只读）" },
    { "step": 2, "form": "leave_request_form",        "desc": "抽取起止/事由，时长自动计算，表单确认（HITL）" },
    { "step": 3, "tool": "oa__submit_leave_request",  "idempotency": true }
  ],
  "form": { /* 字段、校验、默认值来源 —— 同 5.2 表单定义 */ },
  "permissions": ["oa:leave_request:create", "oa:leave_balance:read"],
  "output": { "card": "leave_submitted_card", "exports": [] }
}
```

#### 3.2.3 技能注册与发现

- 技能注册到 Agent Core 的 **Skill Registry**，与 MCP `tools/list` 的 Tool Registry 并存：Route 阶段优先匹配技能（问法/描述相似度），未命中技能再降级到裸工具
- 技能的 `trigger.utterances` 作为意图识别的增强样本，持续提升命中率
- 技能不可绕过 Agent Core 流水线：执行仍走 4.1 的权限校验 → Schema 校验 → HITL → 幂等机制

### 3.3 三种技能执行模式

> 参考 WorkBuddy 的"问一问 / 做一做 / 想一想"三模式设计，控制风险暴露程度。

| 模式 | 行为 | 风险等级 | 适用场景 |
|------|------|---------|---------|
| **只问不写（Ask）** | 只调用只读工具（`readOnlyHint: true`），无写入，无需确认卡 | 低 | 查询、统计、报表导出 |
| **先计划后执行（Plan）** | 先展示**执行计划卡**（将调用哪些系统、读改哪些数据、影响范围），用户批准后逐步执行 | 中 | 跨系统编排、批量处理、周报生成 |
| **直接执行（Craft）** | 表单确认卡（HITL）后立即执行 | 高（有确认卡兜底） | 单据录入（默认模式） |

**规则：**
1. 每个技能声明默认模式；用户可临时切换（如"先给我看下你要做什么"→ 切 Plan）
2. 涉及写入的步骤，无论什么模式都**强制走 HITL 确认卡**（模式只影响是否先出全局计划，不豁免单步确认）
3. 科室自定义技能的 mode 只能是 Ask 或 Plan（写权限技能的 Craft 模式仅限官方审核过的技能）

### 3.4 技能分类与技能市场（Skill Store）

| 类别 | 创建者 | 审核要求 | 共享范围 |
|------|--------|---------|---------|
| **官方技能** | 信息科 | 上线评审 + 测试集回归（16 章） | 全集团 |
| **科室技能** | 科室管理员（低代码搭建：表单拖拽 + 工具编排） | 含写入工具的必须经信息科审核 | 本科室默认；可申请跨科室共享 |
| **个人快捷技能** | 员工个人 | 无需审核（仅限只读工具或本人数据） | 仅本人 |

**技能市场功能（Phase 2 上线，MVP 仅提供"我的技能"列表）：**

- **技能广场**：按科室/场景浏览搜索，展示评分、安装量、更新时间
- **一键安装**：安装即校验权限，权限不足显示原因（不可安装）
- **技能详情页**：功能说明、示例问法、所需权限、数据访问范围、版本日志
- **上架流程**：科室管理员提交 → 信息科安全评审（重点：写入类工具、数据范围）→ 发布
- **使用统计**：调用量、成功率、确认卡修改率，反哺技能优化（与 16.4 灰度观测联动）

### 3.5 技能生命周期

```
创建（低代码/JSON）→ 沙箱试运行（只读真实数据 + 写入到测试环境）
  → 审核上架 → 版本管理（语义化版本，灰度发布）
  → 运行监控 → 下架/归档（历史调用记录保留审计）
```

- **版本管理**：技能升级不影响已安装用户的在用版本，用户确认后升级；破坏性变更（表单字段变化）需二次确认
- **沙箱试运行**：科室技能上架前必须在沙箱跑通全部触发问法（每问法 ≥ 3 次通过）

### 3.6 技能触发与自动化任务

> 参考 WorkBuddy 自动化设计：自动化 = 由技能 + 调度规则 + 推送渠道组成的**无人值守任务**，是技能的第二种执行形态（第一种是对话触发）。

#### 3.6.1 触发方式总览

| 触发方式 | 说明 | 阶段 |
|----------|------|------|
| **对话触发** | 用户自然语言命中技能（MVP 唯一方式） | MVP |
| **定时触发（自动化）** | 按调度规则自动执行技能并推送结果 | Phase 2（对应 6.3） |
| **事件触发** | 订阅业务系统事件（库存低于阈值、合同到期、审批超时）→ 触发技能 → 推送 | Phase 2 |
| **手动编排触发** | 用户把多个技能串成个人工作流 | Phase 3（对应 7.3 自主工作流） |

#### 3.6.2 自动化任务（Automation Task）设计

**任务配置结构：**

```jsonc
{
  "taskId": "auto_001234",
  "name": "每日销售早报",
  "skill": "bi_query",                  // 挂载的技能（或技能编排的工作流）
  "params": { "query": "昨日销售额、Top5 客户、待审批数" },  // 固定参数
  "schedule": {                          // 调度规则（多规则合并，不拆多个任务）
    "type": "weekly",
    "days": ["mon", "tue", "wed", "thu", "fri"],   // 每周多天
    "time": "08:30",
    "effectiveRange": { "from": "2026-10-01", "to": null }  // 生效区间
  },
  "channel": {                           // 结果推送渠道
    "push": ["wecom_bot", "chatwork_session"],   // 企微机器人 / Chat-Work 会话
    "format": "card"                     // card | file | summary
  },
  "permMode": "ask",                     // 自动化任务权限模式：ask | plan（见下）
  "owner": "EMP002345",                  // 创建者（以本人身份执行）
  "scope": "personal"                    // personal | dept（科室级需管理员审批）
}
```

**调度规则能力（对齐 WorkBuddy）：**

| 规则类型 | 示例 | 说明 |
|---------|------|------|
| 每天执行 | 每天 08:30 | 含"仅工作日"选项（自动跳过法定节假日，需接入节假日日历） |
| 每周多天 | 每周一/三/五 09:00 | **多天合并在一条规则**，不拆多个任务 |
| 每月多天 | 每月 1/15/28 日 09:00 | 同上 |
| 单次执行 | 2026-10-10 14:00 一次 | 提醒类一次性任务 |
| 事件触发 | 库存 < 阈值 / 合同到期前 7 天 | Phase 2，见 3.6.1 |

**执行约束（防护栏）：**

| 约束 | 限制 | 说明 |
|------|------|------|
| 任务频率 | 单任务 ≥ 15 分钟间隔 | 防高频空转 |
| 最大执行时长 | 30 分钟，超时自动终止并通知 | 防卡死占用资源 |
| 并发控制 | 单用户同时运行 ≤ 3 个自动化任务；全系统按 Token 预算排队 | 保护 LLM 资源 |
| 失败重试 | 失败后不自动重试（区别于幂等写入），次日正常触发；连续 3 次失败自动暂停并通知创建者 | 防错误结果误导 |
| 权限模式 | 自动化任务**只能用 Ask / Plan 模式**，禁止 Craft | 无人值守时绝不允许无人在场写入 |

**执行身份与权限：**
- 自动化任务以**创建者本人身份**执行（权限随人走）；创建者调岗/权限变更后，任务下次执行自动按新权限收敛
- 创建者离职 → 其个人自动化任务自动停用（与 8.5.6 生命周期联动）

**上线流程（先跑通再自动化，对齐 WorkBuddy 实践）：**

```
① 用户对话中手动执行该技能并跑通 → ② 一键"转为自动化"（带入当前参数）
  → ③ 配置调度与推送渠道 → ④ 首次执行结果推送创建者确认 → ⑤ 正式启用
```

**自动化模板库：** 预置常见场景模板（每日销售早报、周报、审批超时日报、库存预警、合同到期提醒），用户选模板改参数即可，无需从零配置。

**自动化管理界面：**
- 任务列表：状态（运行中/已暂停/失败）、下次执行时间、最近一次结果
- 执行历史：每次执行的产物、耗时、Token 消耗、成功/失败
- 自然语言管理："把早报改成 9 点发"、"暂停库存提醒"、"这个月结束就停掉"

**知识库联动（详见 9.5）：** 自动化任务的参数支持从知识库引用（如周报模板、简报内容结构），模板更新即全量生效。

### 3.7 MVP 官方技能清单

MVP 上线 **4 个官方技能**，与 5.2 场景一一对应（场景即技能的实例化验收）：

| skillId | 名称 | 对应场景 | 默认模式 | 核心编排 |
|---------|------|---------|---------|---------|
| `oa_leave_request` | 请假审批 | 场景 1-1 | Craft | 查余额 → 抽取起止/事由 → 时长自动计算 → 表单确认 → 提交 → 返回审批单号 |
| `oa_todo_approve` | 待办审批 | 场景 2 | Craft | 聚合待办 → 列表卡片 → 详情 → 二次确认 → 审批/驳回 + 通知 |
| `bi_query` | BI 数据查询 | 场景 3 | Ask | 意图转查询 → 执行 → 数值/图表卡片 → 可追加维度 |
| `weekly_report` | 工作周报生成 | 场景 4 | Plan | 汇总操作记录 + 待办 + BI 摘要 → 生成草稿 → 用户编辑 → 导出/发送 |

> 销售订单录入技能（`crm_sales_order_entry`）属 CRM 对接，Phase 2 上线（见 6.1.1）。

---

## 4. Agent Core 架构设计

> Agent Core 是整个系统的大脑，负责理解用户意图、选择工具、编排执行。以下流程设计必须在 MVP 阶段就明确，因为它直接决定系统可行性。

### 4.1 核心处理流水线

```
用户输入
   │
   ▼
┌──────────────┐    不明确时     ┌──────────────┐
│  Intent      │──────────────▶ │ Clarify      │
│ Recognition  │                │ 追问澄清      │
└──────┬───────┘                └──────┬───────┘
       │ 明确                            │
       ▼                                │
┌──────────────┐                        │
│  Route       │                        │
│ 选工具+系统   │                        │
└──────┬───────┘◀───────────────────────┘
       │
       ▼
┌──────────────┐    缺字段时     ┌──────────────┐
│  Parameter   │──────────────▶ │ 字段补全     │
│ Extraction   │                │ 对话追问      │
└──────┬───────┘                └──────────────┘
       │ 完整
       ▼
┌──────────────┐
│  Schema      │  ← 强类型校验，防 LLM 幻觉
│ Validation   │
└──────┬───────┘
       │ 通过
       ▼
┌──────────────┐    写入类操作    ┌──────────────┐
│ Permission   │──────────────▶ │ 人在回路     │
│ Check        │                │ 确认卡二次确认│
└──────┬───────┘                └──────┬───────┘
       │ 通过                           │ 确认
       ▼                                │
┌──────────────┐ ◀─────────────────────┘
│  Execute     │  ← MCP Client 中间件链（权限/审计/HITL）
└──────┬───────┘
       │
       ▼
┌──────────────┐
│  Format      │  ← 结果转自然语言 + 卡片展示
│  Response    │
└──────┬───────┘
       │
       ▼
  展示给用户
```

### 4.2 各阶段设计细节

#### 阶段 1：Intent Recognition（意图识别）

| 项目 | 设计 |
|------|------|
| 输入 | 用户当前输入 + 近 10 轮会话记忆 + 用户档案 + 已连系统列表 |
| 输出 | `{ intent: string, confidence: float, candidate_tools: Tool[] }` |
| 判定 | confidence ≥ 0.8 直接进入路由；0.5~0.8 触发澄清追问；< 0.5 回复"我没理解" |
| 歧义消解示例 | 用户说"查一下这个月的数据" → Agent 反问"请问是哪个系统的数据？销售 / BI / ERP？" |

#### 阶段 2：Route（工具路由）

- **技能优先路由**：先在 Skill Registry 中按触发问法/描述匹配技能（见 3.2.3）；命中技能则按技能编排（flow）执行；未命中再降级到裸工具路由
- **工具来源**：启动时通过 MCP 协议（`tools/list`）从所有 MCP Server 自动发现，动态构建 Tool Registry，支持热插拔
- 每个工具的标准字段：`name`、`description`、`inputSchema`；HITL 与权限标识从 `annotations`（含 `_meta` 扩展）读取（规范见 8.3）
- LLM 根据工具描述选择，配合**权限白名单**过滤掉用户无权调用的工具
- 不允许 LLM 调用 Tool Registry 以外的任何工具，根绝 Prompt 注入

#### 阶段 3：Parameter Extraction（参数提取）

- 从用户输入 + 会话记忆中提取参数值
- 参数必须符合目标 MCP 工具的 `inputSchema`（严格 JSON Schema，字段名/类型/枚举/必填项）
- 缺失必填参数时，生成针对性追问（"请提供客户名称" 而非 "还需要一些信息"）
- **值对齐**：用户说"XX 公司"时，调用 MCP Server 提供的枚举工具（如 `oa.list_customers`）做模糊匹配，取精确 ID

#### 阶段 4：Schema Validation（Schema 校验）

- LLM 输出先经过**严格的 JSON Schema 校验器**（不是让 LLM 自己再检查一遍），Schema 来自 MCP 工具的 `inputSchema`
- 校验失败 → 返回提取阶段重试（最多 2 次），仍失败则回复"我暂时无法准确提取信息"
- 防幻觉：字段值必须在 MCP Server 返回的枚举工具结果中存在，否则视为无效

#### 阶段 5：Permission Check（权限校验）

```
权限校验 = F(用户角色, 所属科室, 目标系统, 目标工具, 参数中的数据范围)
```

- 查询参数中的数据范围（如 "华东区"、"销售一科"）必须在用户权限范围内
- 写入操作额外检查操作级权限（录入 vs 提交 vs 审批）
- 校验失败 → 返回友好提示，**不暴露敏感信息**（如不说"你没有审批权限"而说"该操作需要审批权限，请联系主管"）

#### 阶段 6：Execute（执行）

- 通过 MCP 协议调用对应 MCP Server 的 `tools/call`
- 请求 header 透传 AuthContext（userId、科室、角色）
- 设置超时：查询类 10s，写入类 30s
- 失败处理见 4.3 错误处理表

#### 阶段 7：Format Response（结果格式化）

| 查询结果 | 展示方式 |
|----------|----------|
| 单值 | "本月华东区销售额为 **¥1,234,567**，较上月 +5.2%" |
| 列表（≤10 条） | 表格卡片 |
| 列表（>10 条） | 分页表格 + 下载按钮 |
| 图表数据 | 内嵌迷你图表（ECharts） |
| 写入结果 | ✅ 确认卡 → ✅ 成功提示 + 单据编号 |

### 4.3 错误处理与降级策略

| 错误类型 | 检测位置 | 处理方式 | 用户可见反馈 |
|----------|----------|----------|-------------|
| LLM 输出非 JSON | Schema Validation | 最多重试 2 次，换更严格 prompt | "我正在分析，请稍等…" → 失败时"抱歉，我没理解你的意思" |
| 参数提取不完整 | Parameter Extraction | 追问缺失字段 | "请提供一下客户名称" |
| 意图不明确 | Intent Recognition | 澄清追问 | "请问是要查询销售数据还是采购数据？" |
| 权限不足 | Permission Check | 拒绝并引导 | "该操作需要审批权限，请联系主管" |
| MCP Server 超时 | Execute | **查询类**：重试 1 次；**写入类**：不盲目重试，按幂等键查询实际结果后再决定（见 8.7） | "系统响应较慢，正在确认处理结果…" |
| MCP 调用返回错误 | Execute | 记录日志；向用户展示可读错误 | "录入失败：客户编号在 OA 中不存在" |
| MCP Server 不可用 | Execute | 标记该 Server 为临时不可用（熔断 5 分钟） | "OA 系统暂时无法连接，请稍后再试" |
| LLM 服务不可用 | 全局 | 降级为**固定指令集模式**（FAQ + 常用查询模板） | "AI 服务临时维护中，您可以使用以下快捷操作" |

---

## 5. MVP 功能（Phase 1 — 3 个月）

> MVP 聚焦 **2 个系统 + 2 个科室（销售科、采购科）+ 4 类高频场景**：单据录入（2 种表单）、待办审核、BI 查询、周报生成。

### 5.1 登录与组织（基础能力，首期必须）

| 编号 | 功能 | 说明 |
|------|------|------|
| A-01 | SSO 统一登录 | 对接公司 AD（Keycloak 统一认证，见 8.5）：系统默认浏览器授权 + Loopback 回调，免二次输入 |
| A-02 | 组织自动加入 | 登录后根据 HR 主数据自动关联所属科室/部门 |
| A-03 | 个人档案 | 员工查看本人角色、权限范围、已连接系统 |
| A-04 | 会话管理 | 新建/历史对话、对话标签、对话分享给同事 |
| A-05 | 我的技能 | 已安装技能列表、技能使用记录、执行模式切换（技能市场见 Phase 2） |
| A-06 | 桌面端客户端 | Electron：安装分发、系统托盘、系统通知、Quick Ask、本地文件授权、自动更新（见 5.5） |

### 5.2 MVP 场景明细（聚焦请假审批单据）

> **收窄原则**：MVP 的 OA 侧聚焦**流程审批**：**请假审批**一种表单（带完整字段和校验规则）做深做透，加上待办审批处理；其他场景（BI 查询、周报）也在 MVP 内。**报销审批、请购单录入随 Phase 2 OA 深化纳入**（见 6.1.2，表单定义与交互模式复用本节场景 1-1 的表单确认卡）。**销售订单录入属 CRM**（集团销售下单/跟单系统），随 Phase 2 CRM 对接纳入（见 6.1.1）。快速跑通端到端闭环。
>
> 每个场景对应一个官方技能（技能清单与编排见 3.7），场景验收 = 技能的端到端验收。

#### 场景 1-1：OA 请假审批（全员，技能 `oa_leave_request`）

**目标表单**：OA 请假申请单 v3.0，关键字段如下

| 字段 | 类型 | 必填 | 校验规则 | 默认值来源 |
|------|------|------|----------|------|
| 请假类型 | 枚举 | ✅ | 年假 / 调休 / 事假 / 病假 / 婚假 / 产假 / 陪产假 / 丧假 | 无，意图识别（用户未说必问）；年假/调休需查余额校验 |
| 开始时间 | 日期时间 | ✅ | ≥ 当前时间 | **无默认值，必问**（不可由 Agent 臆造） |
| 结束时间 | 日期时间 | ✅ | > 开始时间 | **无默认值，必问**（Agent 可按时长反推候选让用户确认） |
| 请假时长 | 数值 | ✅ | 按起止时间自动计算（工作日口径，精确到 0.5 天） | 自动计算（用户可改，改动需与起止一致） |
| 请假事由 | 文本 | ✅ | ≤ 200 字符 | **无默认值，必问** |
| 附件 | 文件 | 条件必填 | 病假 ≥ 2 天需医疗证明 | —（本地文件授权读取，见 5.5） |
| 交接人 | 关联 | ❌ | OA 员工库 | 无 |
| 申请人 | 关联 | ✅ | — | 默认当前用户（不问） |
| 申请部门 | 关联 | ✅ | — | 默认当前用户科室（不问） |
| 审批流 | 流程 | ✅ | 按 OA HR 规则自动路由（≥ 3 天加部门总监节点） | OA HR 配置自动路由 |

> **默认值规则**：凡表单标注了默认值来源的字段，Agent 在确认卡中标注"（默认：XX）"并允许一句话修改；**无默认值来源的必填字段（如起止时间、事由）必须追问**，禁止 Agent 自行编造。年假/调休类型时，Agent 先调 `oa__query_leave_balance` 查余额并展示"扣减后剩余"。

**典型对话流**：

```
用户: "我要请两天年假"
Agent: 好的，查到你的年假余额为 5 天（扣 2 天后剩 3 天）。请问从哪天开始？
用户: "下周四，9 月 24 号"
Agent: [确认卡] 请确认以下信息：
       请假类型：年假（余额 5 天，扣减后剩 3 天）
       开始时间：2026-09-24 09:00（默认：工作日上班开始）
       结束时间：2026-09-25 18:00（默认：两天后工作日结束）
       请假时长：2 天（自动计算）
       请假事由：？
       请问请假事由是什么？
用户: "家里有事"
Agent: [确认卡] … 请假事由：家里有事 … [确认] [修改字段]
用户: "结束时间改成 25 号中午 12 点"
Agent: [更新后确认卡] 请假时长：1.5 天（自动重算） …
用户: "确认"
Agent: ✅ 请假单 LE20260916001 已提交，审批流：直属主管 → HR 备案（< 3 天免部门总监）
```

#### 场景 2：待办查询与审核（覆盖 MVP 科室主管，技能 `oa_todo_approve`）

| 用户故事 | 验收标准 |
|----------|----------|
| 作为主管，我要"看看我今天有哪些待审批" | Agent 聚合 OA 待办 → 按紧急度排序 → 展示卡片列表 |
| 我要说"同意第 3 条" | Agent 展示第 3 条详情 → 二次确认 → 调用审批接口 → 返回结果 |
| 我要说"这条驳回，原因是预算超了" | Agent 提取驳回原因 → 调用驳回接口 → 通知申请人 |

#### 场景 3：BI 数据查询（覆盖所有 MVP 科室，技能 `bi_query`）

| 用户故事 | 验收标准 |
|----------|----------|
| 作为销售，我要"本月华东区销售额多少" | Agent 生成 BI 查询 → 返回数值 + 小趋势图 |
| 我要说"按产品类别拆分一下" | Agent 追加维度 → 返回饼图/柱状图 |
| 作为采购主管，我要"导出 Q3 各供应商采购金额明细" | Agent 生成 Excel → 提供下载链接 |

> 注：应收账龄等财务类查询在财务科接入后开放（Phase 2 起评估）。

#### 场景 4：个人工作周报自动生成（技能 `weekly_report`）

| 用户故事 | 验收标准 |
|----------|----------|
| 作为员工，我要说"帮我生成本周工作周报" | Agent 聚合本周 OA 操作记录 + 待办完成情况 + BI 数据摘要 → 生成 Markdown → 用户编辑后发送至指定群 |

> **前置依赖（需提前确认）**：
> ① OA 需提供"按人查询操作日志"的 API（周报数据源）
> ② IM 集成（企微/钉钉群消息接口）用于"发送至指定群"——若 MVP 无法打通，降级为"生成后下载/复制"，发送功能移至 Phase 2

### 5.3 MVP 非功能要求

| 项目 | 要求 |
|------|------|
| 对话响应延迟 | 平均 ≤ 2s，P95 ≤ 5s |
| 系统可用性 | 99.5%（工作时间） |
| 并发支持 | 500 人同时在线 |

### 5.4 交互原型

配套高保真交互原型：[prototype.html](file:///d:/prj/chat-work/prototype.html)（单文件，浏览器直接打开）。

| 原型覆盖 | 对应 PRD 章节 |
|---------|--------------|
| 三栏布局：会话/技能侧栏 + 对话主区 + 上下文面板（技能流程/记忆引用/客户端能力） | 3.1 功能架构 |
| 场景 1 流程表单提交（以请假审批为演示载体）：思考与执行折叠块（意图→技能路由→参数→校验）、表单确认卡（字段来源徽标：默认/追问获得/自动计算）、提交后流水线步骤（权限校验→幂等键→写入→审批流）——同款交互适用于 Phase 2 OA 报销/请购扩展与 CRM 订单录入 | 4.1 Agent Core、5.2 场景 1-1、6.1.1/6.1.2、8.7 幂等 |
| 场景 2 待办审批：审批列表卡、同意操作行内二次确认（HITL） | 5.2 场景 2、10.1 |
| 场景 3 BI 查询：KPI 卡 + 产品线条形图 + 权限范围过滤提示，只读模式（Ask） | 5.2 场景 3、2.3 权限矩阵 |
| 执行模式切换（只问不写/先计划后执行/直接执行）+ 模式不豁免确认提示 | 3.3 三种执行模式 |
| 顶栏 Quick Ask（Alt+Space）、桌面端本地文件徽标、附件本地 MCP 标记 | 5.5 桌面端 |
| 右侧记忆面板（常用客户/偏好/习惯）+ 管理入口 | 9.1 记忆分层 |
| Toast 反馈（操作结果/模式切换说明） | 11.4 可观测性（用户反馈） |
| SSO 登录页：AD/企微/钉钉三渠道 + 加载跳转动效 + HR 主数据关联提示 + 安全脚注 | 8.5 SSO 认证（Identity Broker / OIDC+PKCE / 免登） |
| 技能市场：统计条、分类 Tab（官方/科室/个人）、技能卡（模式徽章/评分/安装量/示例问法）、安装交互（权限不足不可安装）、上架流程入口；**上架技能三步向导**（基本信息/发布范围 → 执行模式 → 调用工具与数据范围声明（只读/写入徽标）→ 提交信息科安全评审 → 模拟评审通过发布上架，评审规则说明、市场「评审中」卡片与统计联动） | 3.4 技能分类与市场、3.7 官方技能清单 |
| 知识库：空间 Tab、文档列表（类型/密级 D1-D3/生效状态/引用次数/来源）、失效文档向量下线提示、知识缺口分析、RAG 注入约束说明 | 9.5 知识库设计 |
| 自动化任务：防护栏警示条、任务卡（调度规则/下次执行/最近结果/暂停开关）、连续失败自动暂停示例、模板任务、执行历史（耗时/Token）；**模板库弹层**（6 模板卡：技能/调度/推送标签、已在用状态、一键套用预填）；**新建任务四步向导**（来源三选：会话转换（推荐，技能/参数自动带入）/模板创建（联动模板库）/空白新建 → 调度规则与推送渠道配置（事件触发隐藏时间、模式锁定防护栏）→ 首次试运行结果 HITL 确认 → 启用并插入任务卡与执行历史） | 3.6 自动化任务设计 |
| 管理我的记忆：记忆分层示意（L1/L2/L3）、PIPL 知情同意开关（关闭仅留 L1）、记忆条目（来源/引用次数/行内二次确认删除）、敏感隔离说明、危险区一键清除 | 9.1-9.3 记忆分层/边界/写入检索 |
| 设置：个人档案（角色/已连接系统/权限范围/perm_ver）、执行偏好（默认模式/HITL 锁定/幂等锁定）、通知开关、记忆与知识联动、安全（审计日志/活跃会话/数据分级/退出登录） | 5.1 A-03、2.3 权限矩阵、10.2 数据分级 |
| ERP 复杂单据录入：物料歧义候选选择卡（Top N + 编码/规格/库存，必选不猜）、单据摘要卡（组数/金额/字段数/异常警示）、diff 变更卡（旧值删除线→新值、金额联动重算、大额阈值提示）、全屏单据工作台（主表 86 字段分组折叠 + 字段来源徽标、子表 12 行虚拟滚动 + 仅看异常行过滤、孙表批次懒加载钻取）、提交成功流水线（三级写入/异常留痕/分级审批） | 6.1 ERP 对接（Phase 2 复杂单据演示）、8.7 幂等 |
| 管理后台视图：评审工作台（技能评审队列 + 评审单详情 + 通过/驳回留痕）、审计日志（筛选 + 导出）、知识库审核（涉密检测报告 + D2 复核）、用户与权限（perm_ver 触发 + 生命周期联动）；标注"桌面端角色菜单（MVP）→ 独立 Web 后台（Phase 2）"分阶段形态 | 5.6 管理端（角色矩阵/工作台清单/复用架构/管理操作约束） |

> 原型为静态演示（模拟数据 + 少量 JS 交互），用于评审界面结构与交互模式，不代表最终视觉稿；原型以浏览器呈现演示，实际界面运行于 Electron 桌面端主窗口（见 5.5）。

### 5.5 桌面端客户端（Electron，MVP 唯一客户端）

> **客户端形态决策：MVP 起仅提供 Electron 桌面端，独立 Web 端取消。** 理由：①企业内网由信息科统一安装分发，Web 的"零安装"优势不成立；②托盘常驻、本地文件、全局快捷键是核心体验，浏览器无法提供；③单一客户端避免双端开发、浏览器兼容矩阵与安全维护成本；④渲染层独立于 Electron 壳，未来如确需 Web 版可基于同一渲染层快速补齐。

#### 5.5.1 桌面端核心能力

| 能力 | 说明 |
|------|------|
| **托盘常驻提醒** | 系统托盘 + 系统级通知：自动化任务结果、审批待办、确认卡片（可直接点开处理） |
| **本地文件操作** | 授权目录读写：周报/BI 报表**直接落盘**到用户指定目录、拖拽上传附件、批量整理 |
| **全局快捷键** | `Alt+Space` 全局唤起 Quick Ask 小窗（类 Spotlight），随时提问不打断当前工作 |
| **弱网体验** | 会话历史/知识文档本地缓存（SQLite），内网弱网可查历史 |
| **离线自动化** | 任务在服务端跑（关机不影响）；桌面端负责**结果接收与提醒**（同 3.6.2） |
| **信创国产 OS** | 统信 UOS / 银河麒麟原生安装包，覆盖国产化要求 |

#### 5.5.2 技术选型

| 项 | 主方案 | 理由 |
|----|--------|------|
| 框架 | **Electron** | 渲染层（React + Ant Design）独立于 Electron 壳；生态成熟（自动更新/崩溃收集/代码签名开箱即用）；统信/麒麟官方支持 Electron 打包 |
| 备选 | Tauri | 包体小（~10MB vs ~150MB）、内存低；但后端为 Rust（团队栈为 Python/TS），且信创兼容验证成本高——作为长期演进备选 |
| 本地存储 | SQLite（better-sqlite3）+ OS 密钥链 | 会话缓存/配置本地化；token 走 DPAPI/Keychain 加密 |
| 自动更新 | electron-updater + **内网更新服务器**（私有 feed，HTTPS） | 分灰度通道（与 16.4 灰度观测联动），不上外网 |
| 崩溃收集 | 自建 Sentry 兼容服务（crashpad） | 崩溃报告不出内网 |

#### 5.5.3 进程架构与安全红线

```
┌─────────────────────────────────────────────┐
│ 主进程（Node.js，唯一可信层）                  │
│  窗口管理 / 系统托盘 / 全局快捷键 / 自动更新     │
│  本地 MCP Server（stdio，见 5.5.5）           │
├─────────────────────────────────────────────┤
│ preload 桥（contextBridge）                   │
│  仅暴露白名单 IPC API：文件选择/落盘、通知、    │
│  打开目录、获取版本 —— 枚举式注册，无动态通道   │
├─────────────────────────────────────────────┤
│ 渲染进程（Chromium = React 渲染层）            │
│  无 Node 能力，仅经 preload 白名单 IPC        │
└─────────────────────────────────────────────┘
```

**安全红线（企业强制）：**

| # | 要求 |
|---|------|
| 1 | `contextIsolation: true` + `nodeIntegration: false`——渲染进程零 Node 能力，所有系统能力走 preload 白名单 |
| 2 | IPC 通道枚举式注册，禁止动态通道名；参数走 zod schema 校验 |
| 3 | 本地文件工具的目录访问**单独弹窗授权**（首次使用选择目录，记录授权范围，越界直接拒绝），与 3.3 权限模式对齐 |
| 4 | 产物落盘前扩展名白名单校验（.xlsx/.docx/.pdf/.md…），防恶意文件名写盘 |
| 5 | 安装包代码签名（Windows 企业证书；外网分发时 macOS 公证）；更新包强制校验签名与哈希，防更新劫持 |
| 6 | 本地 SQLite 缓存数据加密（AES，密钥存 OS 密钥链）；卸载时数据目录一并清理 |

#### 5.5.4 登录与会话（SSO 集成，见 8.5.8）

- 桌面端登录走**系统默认浏览器 + Loopback 回调**（不用内嵌 webview——防 IdP Cookie 被内嵌环境劫持；完整时序见 8.5.8）
- Token 存 OS 安全存储（Windows DPAPI / macOS Keychain），不落明文文件
- 已登录态静默续期；托盘常驻期间保活会话（超 8 小时 refresh 过期则托盘图标置灰提示重新登录）

#### 5.5.5 本地 MCP Server（桌面端特有能力）

桌面端内置一个 stdio MCP Server，提供本地文件工具，注册方式与服务端 MCP 一致：

| 工具 | 说明 | 约束 |
|------|------|------|
| `local__select_file` / `local__select_dir` | 系统选择器，返回用户选择的路径 | 首次授权 |
| `local__read_file` | 读授权目录内文件（xlsx/csv/docx/pdf 解析） | 仅授权目录 + 扩展名白名单 |
| `local__write_file` | 产物落盘（周报、BI 报表导出） | 同上；覆盖已有文件需确认卡 |
| `local__list_dir` | 列授权目录结构 | 仅授权目录 |

- 这些工具**仅在该用户的桌面会话中可见**：Agent Core 路由时感知"客户端能力标记"（桌面端请求带 `X-Client-Capabilities: local-file`）；非桌面客户端（Phase 3 小程序）不出现本地工具，避免路由幻觉
- 服务端托管的自动化任务**不能调用本地工具**（无人值守时不碰用户磁盘），只产出服务端产物供下载/推送

#### 5.5.6 分发与版本管理

| 项 | 方案 |
|----|------|
| 安装渠道 | 内网下载页（exe/dmg/deb/rpm）+ 统信/麒麟应用商店上架 |
| 灰度更新 | 内网更新服务器分通道（stable/beta），按科室灰度（与 16.4 联动） |
| 强制升级 | 出现安全漏洞版本时，登录网关返回 `client_version_too_low` 强制阻断 |
| 最小支持版本 | 桌面端与服务端 API 版本协商：客户端带版本号，网关按兼容矩阵放行 |

#### 5.5.7 桌面端功能清单（MVP 交付）

| 功能 | 说明 |
|------|------|
| 主窗口 | 会话/技能/审批（知识库 Phase 2 上线后进入主窗口） |
| 系统托盘 | 常驻图标：未读提醒角标、右键菜单（新建对话/暂停通知/退出） |
| 系统级通知 | 自动化结果、审批待办、确认卡片（点击直达会话） |
| Quick Ask 小窗 | 全局快捷键唤起，轻量问答（只读意图），`Esc` 关闭 |
| 本地文件 | 拖拽上传、产物落盘、授权目录管理 |
| 自动更新 | 后台下载、重启生效、更新日志展示 |

### 5.6 管理端（分阶段形态：MVP 桌面端角色菜单 → Phase 2 Web 管理后台）

> **形态决策：** 管理功能低频、大屏表格密集、多人协作，与员工对话场景差异大。**MVP 仅有的少量管理动作并入桌面端角色菜单**（不新建客户端形态，"单一客户端"决策不变）；**Phase 2 管理工作台集中后，上线独立 Web 管理后台**（`admin.chat-work.internal`）——复用 5.5.2 预留的"渲染层与 Electron 壳分离"架构与 `packages/ui`，兑现"未来如确需 Web 版可基于同一渲染层快速补齐"的预留。员工主产品 Web 端维持取消不变，Web 形态仅用于管理后台。

#### 5.6.1 管理角色矩阵

| 角色 | 承担者 | 核心职责 | 对应章节 |
|------|--------|---------|---------|
| 系统管理员 | 信息科 | MCP Server 注册/健康探测、模型路由与网关配置、功能开关 | 8.3 / 11 |
| 安全评审员 | 信息科 | 技能上架安全评审（评审单 SEC-RV-*）、工具与数据范围声明核查 | 3.5 |
| 知识库管理员 | 企管科 / 信息科 | 文档入库审核（涉密检测）、D2+ 密级复核、失效下线、知识缺口运营 | 9.5 |
| 组织管理员 | 人力资源科 | 账号生命周期（入职/调岗/离职联动）、组织与权限基线维护（触发 perm_ver 递增） | 8.5.6 |
| 审计员 | 法务科 | 审计日志查询与导出（合规取证，≥180 天留存） | 10 |

> 员工个人资产的管理（我的技能、我的记忆、我的自动化任务）已由员工自助界面覆盖（3.6 管理界面 / 9.2 记忆可感知与管控），不进管理端。

#### 5.6.2 工作台清单与阶段

| 工作台 | 核心功能 | MVP（桌面端角色菜单） | Phase 2（Web 管理后台） |
|--------|---------|---------------------|------------------------|
| 审计日志 | 按人/时间段/动作筛选，参数摘要与结果，导出 | ✅ 最小版（本人可见范围 + 导出） | ✅ 全量（跨部门、法务取证、双人复核导出） |
| 权限版本 | perm_ver 触发递增、权限基线核对、调岗收敛观测 | ✅ | ✅ 增加账号生命周期联动看板 |
| 技能评审 | 评审队列、评审单详情（工具清单/读写声明/数据范围）、通过/驳回留痕 | —（MVP 技能少，走线下评审） | ✅ |
| 知识库审核 | 入库队列、涉密检测报告、D2+ 复核、失效下线 | —（Phase 2 知识库上线） | ✅ |
| 自动化治理 | 全员任务总览、熔断/暂停、配额与间隔策略 | — | ✅ |
| 系统配置 | MCP Server 注册与健康、工具热注册、模型路由、功能开关 | — | ✅ |

#### 5.6.3 复用架构（Web 管理后台增量成本控制）

| 项 | 方案 |
|----|------|
| 部署 | 内网 DNS `admin.chat-work.internal`；APISIX 新增 `/admin/*` 路由；同机复用现有服务端，无新增基础设施 |
| 认证 | Keycloak 同一 realm，管理角色独立 scope（`admin:review` / `admin:audit`…），与桌面端一套账号体系 |
| 代码复用 | React 渲染层（无 Electron 壳）+ `@chat-work/ui` 管理组件 + `@chat-work/api-client` 自动生成 SDK |
| 能力边界 | 管理后台不调用 local-mcp 本地工具、不承载员工对话功能，纯管理场景 |

#### 5.6.4 管理操作自身约束

- **审计者也被审计**：管理端全部操作写入同一审计流水（谁在何时改了什么权限/放行了什么文档）
- 双人复核项：权限基线变更、D2+ 知识放行、技能评审通过
- Web 后台会话 TTL 30 分钟无操作自动锁定，敏感页面不落浏览器缓存，导出文件带水印与操作人标记

---

## 6. Phase 2 功能（3-6 个月）

### 6.1 新增系统对接

| 系统 | 对接方式 | 优先场景 |
|------|----------|----------|
| CRM | OpenAPI | 销售订单录入（表单确认/幂等写入，提交审批——原 MVP 场景 1-1 迁入，见 6.1.1）、跟单（订单进度跟踪/异常提醒）、客户 360 查询 |
| ERP | OpenAPI + RPA 补充 | 库存查询、采购订单同步、财务凭证摘要 |
| WMS | OpenAPI | 入库/出库单查询、库存预警 |

#### 6.1.1 CRM 销售订单录入（迁自 MVP 场景 1-1，技能 `crm_sales_order_entry`）

> 业务澄清：MVP OA 侧重**请假审批 + 待办审批**（报销/请购单录入随 Phase 2 OA 深化纳入，见 6.1.2），销售订单录入属 **CRM**（集团销售下单/跟单系统），订单录入后提交审批。表单字段、默认值规则、确认卡交互与 MVP 表单场景共用同一套交互模式（字段来源徽标/无默认值必问/幂等写入）。

**目标表单**：CRM 销售订单 v2.0，关键字段（客户主数据、最近成交价改从 CRM 带出）：

| 字段 | 类型 | 必填 | 校验规则 | 默认值来源 |
|------|------|------|----------|------|
| 单据类型 | 枚举 | ✅ | 仅允许"标准销售订单"/"样品单" | 默认"标准销售订单" |
| 客户名称 | 关联 | ✅ | 必须在 CRM 客户库中存在 | 无，必问（模糊匹配列候选） |
| 客户联系人 | 文本 | 条件必填 | 多联系人时必填（Agent 列候选）；仅 1 个自动带出 | CRM 客户主数据 |
| 收货地址 | 文本 | ✅ | ≤ 200 字符 | CRM 客户主数据默认地址，用户可改 |
| 商品明细 | 子表 | ✅ | ≥ 1 行：商品编码、数量（正整数）、单价（正数 2 位小数）、金额（数量×单价自动计算） | 单价默认最近一次成交价（用户可改） |
| 交货日期 | 日期 | ✅ | ≥ 今日 | **无默认值，必问**（不可由 Agent 臆造） |
| 付款方式 | 枚举 | ✅ | 预付 / 月结 30 天 / 月结 60 天 | CRM 客户主数据付款条件（用户可改） |
| 业务员 | 关联 | ✅ | — | 默认当前用户 |
| 提交审批 | 流程 | ✅ | 录入后自动走 CRM 审批流（大额加销售总监节点） | CRM 审批规则自动路由 |

**典型对话流**（与 MVP 表单场景同款交互）：

```
用户: "帮我录一张销售订单，XX 公司，商品 A 100 件，单价 50"
Agent: 好的，匹配到 CRM 客户库 3 条"XX 公司"，请选择正确的：
       ① XX 贸易（上海）有限公司 ② XX 实业（苏州）… ③ XX 进出口…
用户: "第 1 个"
Agent: [确认卡] 请确认以下信息：
       客户：XX 贸易（上海）有限公司
       客户联系人：张三（默认：客户主数据唯一联系人）
       商品：A 100 件 × ¥50 = ¥5,000
       收货地址：上海市浦东新区 XX 路（默认：客户主数据）
       付款方式：月结 30 天（默认：客户付款条件）
       交货日期：？
       请问交货日期是哪天？
用户: "9 月 22 号"
Agent: [确认卡] … 交货日期：2026-09-22 … [确认] [修改字段]
用户: "确认"
Agent: ✅ 销售订单已录入 CRM（SO20260922001）并提交审批：直属主管 → 销售总监（金额 ≥ ¥5,000）
```

#### 6.1.2 OA 表单扩展：报销审批、请购单录入（迁自 MVP 场景 1-2/1-3）

> 迁移说明：MVP 收窄为请假审批单场景（5.2 收窄原则）后，报销审批（技能 `oa_expense_report`）与请购单录入（技能 `oa_purchase_request`）随 Phase 2 OA 深化纳入。表单定义（报销单 v2.1 多行明细/发票张数/大额审批流路由；请购单 v1.5 供应商模糊匹配/最近采购价/预算校验）与交互模式（字段来源徽标/无默认值必问/自动计算联动重算/幂等写入）完整复用场景 1-1 的表单确认卡与确认流水线，评测集同步纳入（原 MVP 评测用例转入本节验收）。

| 技能 | 使用科室 | 关键编排 | 相比请假场景的新增能力点 |
|------|---------|---------|------------------------|
| `oa_expense_report` | 全员（财务科高频） | 明细抽取（发票 OCR 预填）→ 总额自动计算 → 发票张数确认 → 表单确认 → 提交 | 子表多行明细、自动求和联动重算、大额审批流动态路由（> ¥5,000 加节点） |
| `oa_purchase_request` | 采购科 | 匹配供应商（模糊 → 候选）→ 带最近采购价 → 校验预算余额 → 表单确认 → 创建请购 | 关联主数据模糊匹配（供应商库）、预算科目校验、物料明细子表 |

### 6.2 新增科室覆盖

信息科、生产科、计划科、品管科、仓库物流。

### 6.3 新增能力

| 能力 | 说明 |
|------|------|
| **技能市场（Skill Store）** | 技能广场浏览/搜索/一键安装、科室低代码自建技能、上架审核流程（见 3.4） |
| **自动化任务** | 技能的无人值守执行形态：调度规则（每天/每周多天/每月多天/单次/事件）、结果推送（企微 bot/会话/邮件）、执行历史、模板库、约束防护栏（见 3.6.2） |
| **集团与科室知识库** | 知识空间分级、文档上传与审核、RAG 注入、使用统计与知识缺口分析（见 9.5） |
| **跨系统编排** | "CRM 销售订单录入 → 提交审批 → 触发 WMS 备货 → 通知业务员" 一条龙（Plan 模式执行计划卡） |
| **组织记忆 v1** | 沉淀本科室高频问法、常用报表模板、典型审批意见 |
| **多人协同会话** | 多人加入同一对话，共同处理一笔复杂业务 |

---

## 7. Phase 3 功能（6-12 个月）

### 7.1 新增系统对接

MES（生产报工查询）、U8（财务总账辅助查询）。

### 7.2 全部科室覆盖

剩余：人力资源科、企管科、法务科、技术科、产品科。

### 7.3 核心能力升级

| 能力 | 说明 |
|------|------|
| **个人记忆 v2** | 学习每位员工的操作习惯：常用客户、偏好报表格式、审批风格 |
| **组织记忆 v2** | L3 组织记忆与科室知识空间深度联动：制度文档问答、历史案例检索、SOP 自动匹配（见 9.5） |
| **个人知识空间** | 员工个人笔记、常用资料的知识库化，仅本人可注入（见 9.5.1） |
| **自主工作流** | 用户描述目标（如"本月库存周转低于 3 周"），Agent 拆解步骤并执行；支持把多个技能串成个人工作流（见 3.6 手动编排触发） |
| **移动端小程序** | 企业微信/钉钉小程序，支持移动审批和查询 |
| **语音交互** | 支持语音输入，适配生产车间、仓库等场景 |

---

## 8. 系统集成架构

### 8.1 集成策略：两层分离 + MCP 协议标准化

**核心设计：MCP 作为协议层，企业关注点作为中间件层，两层分离。**

```
┌─────────────────────────────────────────────────────────────┐
│                      Agent Core                             │
│              （MCP Client + 权限 + 审计 + HITL）              │
└──────────────────────┬──────────────────────────────────────┘
        MCP 协议（tools/list, tools/call）
        ┌──────────────┴──────────────┐
        ▼                             ▼
┌─────────────────┐          ┌─────────────────┐
│   MCP Server    │          │   MCP Server    │
│   (OA 封装)     │          │   (BI 封装)     │
└────────┬────────┘          └────────┬────────┘
         │                             │
    ┌────┴────┐                  ┌────┴────┐
    │ OpenAPI │                  │ JDBC/API │
    └────┬────┘                  └────┬────┘
         ▼                             ▼
        OA                            BI
```

**为什么选 MCP：**

| 维度 | 说明 |
|------|------|
| **协议标准化** | MCP 定义了工具发现（`tools/list`）、Schema 描述、参数校验、调用（`tools/call`）的标准协议，不需要为每个系统重复设计接口规范 |
| **生态复用** | 社区已有 200+ 现成 MCP Server（Slack、Notion、PostgreSQL、GitHub…），未来集团内部系统也可复用这些生态 |
| **LLM 原生** | MCP 就是为 LLM Tool Calling 设计的，天然适配 Agent Core 的 Route → Execute 流程 |
| **传输灵活** | 支持 stdio、SSE、Streamable HTTP，企业内网可按需选择 |

**MCP 之外必须叠加的企业中间件（MCP 不内置这些）：**

| 能力 | 在哪里实现 | 说明 |
|------|-----------|------|
| 权限校验（多维度） | Agent Core 的 MCP Client 层 | 调用 `tools/call` 前先校验 |
| Human-in-the-Loop | Agent Core 的 MCP Client 层 | 写入类工具调用前弹出确认卡 |
| 操作审计 | Agent Core 的 MCP Client 层 | 每次 `tools/call` 前后写审计日志 |
| 敏感字段脱敏 | MCP Server 内部（封装层） | 在工具的返回结果里按需脱敏 |
| 用户身份透传 | MCP Server 内部（封装层） | 从 MCP 请求 header 中取用户身份 |

### 8.2 业务系统对接方式（底层实现，MCP Server 封装）

MCP 是 Agent ↔ MCP Server 之间的协议，而 MCP Server ↔ 业务系统之间仍然需要不同的对接方式：

| 方式 | 适用场景 | 封装为 MCP Server 的难度 | 备注 |
|------|----------|-------------------------|------|
| **OpenAPI** | OA、CRM、新版 BI | ⭐ 低 | 直接把 OpenAPI endpoint 映射为 MCP tools |
| **JDBC/直连数据库** | U8、老 ERP 只读场景 | ⭐⭐ 中 | SQL 查询封装为 read-only MCP tools，严禁写入 |
| **RPA** | 无接口的老旧系统 | ⭐⭐⭐ 高 | RPA 脚本执行的结果通过 MCP 返回，不适合高频 |
| **文件交换** | MES、WMS 部分场景 | ⭐⭐ 中 | 定时轮询 → 解析文件 → 缓存为 MCP tools 可供查询 |

**总结：MCP 解决的是"Agent 怎么统一调用 MCP Server"，而不是"MCP Server 怎么连业务系统"——后者仍然是上面 4 种方式。**

**遗留系统读写分离规则（详见 ARCHITECTURE 4.2.1）：**

- 老系统按能力分级 L1（OpenAPI）~ L5（仅 Excel 导入导出），**读走最快的路，写必须走"老系统业务认可"的路**（写入要经过老系统自身的校验/审批流/触发器）
- **禁止 DB 直写业务表**（绕过业务逻辑，数据一致性破坏且厂商免责）；确需时只写中间表 + DBA 联合评审
- **RPA 是写入的万能兜底**（走完整界面校验），文件导入写暂存区 + 回读对账
- 降级链：`API → RPA → 文件导入 → 人工兜底`（Agent 仍完成字段提取与校验草稿，用户一键导出后手动录入）
- MCP 封装使底层实现可替换（RPA → API 平滑迁移），Agent Core 与技能零改动

### 8.3 MCP Server 工具注册规范

每个 MCP Server 必须遵守以下命名和 Schema 约定，以便 Agent Core 自动发现和路由：

**命名规范：** MCP 规范要求工具名仅允许 `a-zA-Z0-9_-`（最长 64 字符，**不允许点号**），因此采用**双下划线**分隔系统前缀：

**`{系统标识}__{动词}_{资源}`**

- `oa__submit_leave_request`
- `oa__query_pending_approvals`
- `bi__query_sales_summary`
- `crm__create_sales_order`

**扩展字段规范：** `supportsWrite`、`requiredPermissions` 不是 MCP 标准字段，直接放顶层会被标准 Client 丢弃。必须按 MCP 规范放入 `annotations`：

- 写操作标识 → 标准 `annotations.readOnlyHint: false`（Agent Core 据此决定是否走 HITL）+ `destructiveHint`
- 自定义权限等扩展 → `annotations._meta`（MCP 规范预留的扩展位）

```jsonc
// OA MCP Server 工具声明示例（符合 MCP 规范）
{
  "name": "oa__submit_leave_request",
  "description": "提交请假申请，需要请假类型、起止时间、请假事由等信息",
  "inputSchema": { /* 严格 JSON Schema */ },
  "annotations": {
    "title": "提交请假申请",
    "readOnlyHint": false,          // false = 写入操作，Agent Core 触发 HITL 确认卡
    "destructiveHint": false,        // 不可覆盖已有数据（作废/删除类为 true）
    "idempotentHint": false,         // 非幂等，重试必须携带幂等键（见 8.7）
    "_meta": {
      "requiredPermissions": ["oa:leave_request:create"],
      "idempotencyRequired": true
    }
  }
}
```

```jsonc
// 查询类工具示例
{
  "name": "oa__query_pending_approvals",
  "description": "查询当前用户的待审批列表",
  "inputSchema": { /* ... */ },
  "annotations": {
    "title": "查询待审批",
    "readOnlyHint": true,            // 只读，无需 HITL
    "_meta": {
      "requiredPermissions": ["oa:approval:read"]
    }
  }
}
```

### 8.4 集成网关（MCP Client + 中间件层）

```
┌───────────────────────────────────────────────────────┐
│                   Agent Core                           │
│  ┌──────────────┐    ┌──────────────┐                 │
│  │ Intent +     │───▶│ MCP Client   │                 │
│  │ Route        │    │ (工具发现/调用)│                 │
│  └──────────────┘    └──────┬───────┘                 │
│                              │                         │
│                    MCP 中间件链                          │
│  ┌───────────────────────────────────────────────┐    │
│  │ ① Permission Check  ② HITL 确认  ③ Audit Log  │    │
│  └───────────────────────────────────────────────┘    │
└───────────────────────────────────────────────────────┘
              │ MCP: tools/call
              ▼
┌────────────────────┐     ┌────────────────────┐
│   OA MCP Server    │     │   BI MCP Server    │
│  (OpenAPI 封装)     │     │  (API 封装)         │
└─────────┬──────────┘     └─────────┬──────────┘
          ▼                           ▼
        OA 系统                    BI 系统
```

### 8.5 SSO 认证实现细节

#### 8.5.1 协议选型与 IdP 矩阵

Chat-Work 不直接对接各 IdP 的私有协议，而是引入**身份代理层（Identity Broker）**将所有 IdP 收敛为标准 **OIDC**，Agent Core 只需实现一套 OIDC 客户端逻辑：

| 终端 / 场景 | IdP | 接入协议 | Broker 中的身份源 |
|------------|-----|---------|-----------------|
| 桌面客户端（MVP 唯一客户端，见 5.5） | AD / Entra ID（原 Azure AD） | OIDC + PKCE，**系统默认浏览器授权 + Loopback 回调**（见 8.5.8） | AD 账号（UPN） |
| 移动端小程序（Phase 3） | 企微 / 钉钉 | 企微网页授权 / 钉钉免登（免密授权，见 8.5.3） | 企微 userid / 钉钉 unionid |

**身份代理推荐方案：Keycloak**（开源、支持 OIDC/SAML/CAS/社交 IdP 桥接、支持账号 Federation 到 LDAP/AD）。备选：自研 Spring Authorization Server。

> 收敛为 OIDC 的好处：Agent Core 登录逻辑只有一套；未来集团上线统一认证平台（CAS/IAM）时，只需在 Broker 侧新增一个 Identity Provider，业务代码零改动。

#### 8.5.2 OIDC 授权流程（Authorization Code + PKCE）

> 桌面端为唯一客户端，实际登录时序见 **8.5.8**（系统默认浏览器 + Loopback 回调）。本节描述授权原理（PKCE、code 兑换、state 校验），与 8.5.8 共用同一套 Broker 侧配置。

```
浏览器                 Chat-Work 客户端      Keycloak(Broker)      AD / 企微 / 钉钉
   │                        │                     │                     │
   │ ①访问 chat-work        │                     │                     │
   │──────────────────────▶│                     │                     │
   │ ②未登录→302 跳转授权端点 │                     │                     │
   │◀──────────────────────│                     │                     │
   │ ③展示 IdP 登录页（Broker 按用户所属 IdP 路由） │                     │
   │────────────────────────────────────────────▶│────────────────────▶│
   │ ④用户输入 AD 账密 / 企微扫码免登              │  ④' IdP 校验身份     │
   │◀────────────────────────────────────────────│◀────────────────────│
   │ ⑤重定向回 redirect_uri + code（一次性）       │                     │
   │────────────────────────────────────────────▶│                     │
   │                        │ ⑥后端拿 code+PKCE 校验码 换 token（后端直连，token 不经过浏览器） │
   │                        │────────────────────▶│                     │
   │                        │ ⑦返回 id_token + access_token + refresh_token │
   │                        │◀────────────────────│                     │
   │                        │ ⑧用 工号+邮箱 从 HR 主数据/权限缓存 构建 AuthContext │
   │ ⑨下发会话凭证（桌面端存 OS 安全存储，见 8.5.8） │            │
   │◀──────────────────────│                     │                     │
```

**关键安全要求：**

| # | 要求 | 说明 |
|---|------|------|
| 1 | PKCE 必须 | 防授权码被截获重放（公开客户端场景标准做法） |
| 2 | code 一次性、5 分钟有效 | 用后即焚，防重放 |
| 3 | Token 不落浏览器 | code→token 兑换发生在客户端主进程，浏览器只经手一次性 code |
| 4 | state 参数校验 | 防 CSRF 跳转劫持 |
| 5 | HTTPS 全程 | 授权端点、回调端点强制 TLS |

#### 8.5.3 企微 / 钉钉免登流程（Phase 3 小程序）

用户在企微/钉钉内打开 Chat-Work 小程序（Phase 3）时**免输入账号密码**（桌面端不适用，走 8.5.8 系统浏览器授权）：

```
① 前端检测到企微 UA → 调用企微 JSSDK 获取免登 code
② Chat-Work 后端拿 code + 企业凭证 调企微 API → 换 userid
③ 以 userid 为键向 Keycloak 发起 Broker 登录（OIDC token exchange / 直接签发）
④ 后续流程与 8.5.2 ⑦ 起完全一致
```

钉钉流程相同，仅 code 获取方式和换取的 ID（unionid）不同。

#### 8.5.4 Token 设计

Broker 签发的 access_token 为 **JWT（RS256 签名）**，Chat-Work 后端持有公钥本地验签（不每次远程校验，避免性能瓶颈）：

| Token | 有效期 | 用途 |
|-------|--------|------|
| access_token | 30 分钟 | API 调用凭证；静默刷新，用户无感 |
| refresh_token | 8 小时（工作会话）| 换新 access_token；使用后轮换（旋转复用检测，被盗检测） |
| 会话凭证（OS 安全存储） | 跟随 refresh_token 生命周期 | 桌面端会话保持（DPAPI/Keychain） |

**JWT Claims 结构（示例）：**

```jsonc
{
  "iss": "https://sso.chat-work.internal",   // Broker 签发方
  "sub": "EMP002345",                        // 员工工号（HR 主数据主键，全局唯一）
  "idp": "wecom",                            // 来源 IdP：ad | wecom | dingtalk
  "idp_sub": "wecom_userid_xxx",             // IdP 侧原始 ID
  "dept": "事业部A/销售科",                   // 归属组织（展示用）
  "roles": ["employee", "dept_manager"],
  "perm_ver": 17,                            // 权限数据版本号（见下）
  "iat": 1760000000,
  "exp": 1760001800,
  "jti": "unique-token-id"                   // 用于吊销名单（黑名单）匹配
}
```

> **权限不写入 Token**：完整权限列表（可达 4 层/数百条）过大，Token 仅携带 `perm_ver` 权限版本号；Agent Core 每次构建 AuthContext 时从**权限缓存（Redis，TTL 5 分钟）**读取，权限变更后由管理员触发 `perm_ver` 递增使旧缓存失效——兼顾实时性与 Token 体积。

#### 8.5.5 AuthContext 构建与 MCP 透传

```
① 每个 API 请求：网关验签 JWT（本地公钥）→ 提取 sub(工号)
② Agent Core 用 工号 查权限缓存 → AuthContext
   { userId: "EMP002345", org: "销售科", roles, permissions[] }
③ MCP 请求透传（On-Behalf-Of 模式）：
   HTTP Header: X-Chat-Auth: <JWT>   // MCP Server 持公钥可自行验签
   HTTP Header: X-Chat-Org: 销售科    // 冗余字段仅作展示，鉴权以 JWT 为准
④ MCP Server 调业务系统：
   Service Account（系统账号）+ 在请求体/备注中携带 "代理人: EMP002345"
   → 业务系统单据上保留操作人=系统账号、代理人=员工 双标记，可追溯
```

#### 8.5.6 账号映射（身份主数据）

三类 ID 必须以**员工工号**为枢纽打通：

| ID 体系 | 示例 | 归属系统 |
|---------|------|---------|
| AD UPN | zhangsan@corp.com | AD 域 |
| 企微 userid | wecom_userid_xxx | 企业微信通讯录 |
| 钉钉 unionid | dingtalk_unionid_xxx | 钉钉通讯录 |
| **员工工号** | **EMP002345** | **HR 主数据（权威源）** |
| OA/ERP 等系统账号 | oa_zhangsan | 各业务系统 |

**映射规则：**

1. HR 主数据为权威源：`工号 ↔ 企微userid ↔ 钉钉unionid ↔ AD UPN` 映射表由 HR/信息科维护，**每日凌晨全量同步 + 变更事件实时推送**
2. 登录时若 IdP 身份无法匹配到工号 → 拒绝登录并提示联系信息科（**不自动创建账号**，防止影子账号）
3. 业务系统账号映射（工号 → oa_xxx）由各 MCP Server 的 Service Account 权限配置承载，MVP 阶段 OA 侧人工核对录入

**生命周期联动：**

| 事件 | 触发动作 | 时效 |
|------|---------|------|
| 入职 | HR 创建工号 + IdP 账号 → 映射表生效 → 可登录 | T+1 日 |
| 调岗/换科室 | HR 变更 → 权限版本 perm_ver 递增 → 5 分钟内权限收敛 | ≤ 5 分钟 |
| 离职 | HR 离职单生效 → IdP 账号禁用 + JWT 加入黑名单(jti) + 个人记忆清除（见 9.2） | **实时（事件推送）** |

#### 8.5.7 会话与异常场景策略

| 场景 | 策略 |
|------|------|
| 会话中 token 过期 | 前端静默用 refresh_token 续期；续期失败才跳登录页，对话上下文不丢失 |
| 并发会话数 | 同一用户最多 3 个设备在线；新设备登录踢出最旧会话（审计留痕） |
| 强制下线 | 管理员可按工号吊销：jti 黑名单（Redis，TTL=token 剩余有效期）+ refresh_token 吊销 |
| 时钟偏移 | 验签容差 ±60 秒 |
| Broker 不可用 | 已签发 token 本地验签不受影响（RS256 公钥缓存在网关）；仅新登录失败，降级提示"统一认证服务暂不可用" |
| 登录审计 | 登录/登出/续期/踢出/吊销全量记录：工号、IdP、IP、设备指纹、结果 |

#### 8.5.8 桌面端登录流程（系统默认浏览器 + Loopback 回调）

桌面端（Electron）**不用内嵌 webview 加载 IdP 登录页**（内嵌环境易被 Cookie 劫持/凭据注入，且企微钉钉扫码组件禁止内嵌），改用系统默认浏览器完成授权：

```
桌面客户端              系统默认浏览器         Keycloak(Broker)
   │                        │                     │
   │ ①启动，本地随机端口监听 │                     │
   │   http://127.0.0.1:{port}/callback            │
   │                        │                     │
   │ ②生成 PKCE code_verifier + state，拉起浏览器   │
   │   （redirect_uri = 127.0.0.1 loopback）       │
   │──────────────────────────────────────────────▶│
   │                        │ ③用户在浏览器完成 AD/企微登录（可能已有 SSO 会话，直接免输）│
   │                        │◀────────────────────│
   │                        │ ④浏览器 302 → 127.0.0.1:{port}/callback?code=xxx&state=yyy │
   │                        │─────────┐           │
   │ ⑤桌面端 loopback 收到 code，校验 state        │
   │   （防 CSRF；code 一次性、5 分钟有效）          │
   │ ⑥桌面端拿 code + PKCE verifier 换 token（直连 Broker，token 不经过浏览器进程）│
   │──────────────────────────────────────────────▶│
   │◀──── id_token + access_token + refresh_token ──│
   │ ⑦token 写入 OS 安全存储（DPAPI/Keychain），浏览器展示"登录成功，可关闭此页"回执页│
```

**关键要求：**

| # | 要求 | 说明 |
|---|------|------|
| 1 | Loopback 地址固定为 `127.0.0.1`（非 localhost 域名），端口随机 | RFC 8252 原生应用授权最佳实践 |
| 2 | `state` 校验防 CSRF；回调端口监听仅登录窗口期开启（60 秒超时关闭） | 减少本机攻击面 |
| 3 | Token 兑换发生在桌面主进程，浏览器只经手一次性 code | 与 Web 端"token 不落浏览器"同一原则 |
| 4 | Token 持久化仅走 OS 安全存储（DPAPI/Keychain），禁止明文文件/LocalStorage | 本机其他进程无法读取 |
| 5 | 登录页尾部跳回执页 | 避免用户停留在浏览器授权页困惑 |
| 6 | 企微/钉钉扫码登录复用同一浏览器流程（Broker 侧路由 IdP） | 桌面端无需为每个 IdP 单独适配 |

#### 8.5.9 原有原则（保留）

- 用户**永不向 Chat-Work 提供业务系统密码**
- Service Account + 用户身份透传，双重校验
- 敏感系统（U8、MES）额外增加 IP 白名单

### 8.6 MCP 企业部署注意事项

| 关注点 | 说明 | 应对 |
|--------|------|------|
| **传输层** | MCP 原生支持 stdio/SSE，企业内网建议用 **Streamable HTTP** 或自建 HTTP 网关 | MVP 先用 SSE，Phase 2 切换到 Streamable HTTP |
| **Server 管理** | 每个业务系统一个 MCP Server，需要注册中心 | MCP Server 的 tools 注册到 Agent Core 的 Tool Registry，支持热插拔 |
| **无现成 Server** | OA、U8、MES 等企业系统不会有社区 MCP Server | 每个系统由我们自建 MCP Server 封装层 |
| **高可用** | MCP Server 挂了怎么办 | Agent Core 实现熔断，标记不可用后降级提示"XX 系统暂时无法连接" |
| **权限最小化** | Service Account 在业务系统里只开必要权限 | 每个 MCP Server 对应独立 Service Account，最小权限原则 |

### 8.7 写入操作幂等性设计（防重复录单）

> **超时 ≠ 失败**：写入调用超时后单据可能已创建成功，盲目重试会产生重复单据。这是企业单据场景的致命缺陷，必须从协议层设计防重。

**幂等键机制：**

1. Agent Core 在每次 HITL 确认通过后，生成全局唯一幂等键：`{userId}_{会话ID}_{意图哈希}_{草稿版本号}`
2. 幂等键随 `tools/call` 请求传入 MCP Server（参数名：`_idempotencyKey`）
3. MCP Server 调用业务系统前，先查幂等记录表：
   - 键已存在且成功 → 直接返回原单据编号，**不重复写入**
   - 键已存在且失败 → 返回原失败原因
   - 键不存在 → 执行写入，成功后记录 `幂等键 → 单据编号` 映射（保留 24 小时）
4. 用户在确认卡上修改字段后重新提交 → 草稿版本号 +1 → 生成新幂等键，视为新请求

**超时后的处理流程：**

```
写入调用超时
    │
    ▼
Agent Core 用同一幂等键调用 oa__get_idempotency_result
    │
    ├── 已有结果 → 返回单据编号（告知用户"其实已成功"）
    ├── 无记录   → 询问业务系统该用户当日是否有同参数单据（防底层已写入但记录丢失）
    └── 仍无法确认 → 明确告知用户"结果未知，请到 OA 查看是否已创建，勿重复提交"
```

**配套要求：** 所有写入类 MCP 工具的 `annotations._meta` 必须声明 `idempotencyRequired: true`（见 8.3 示例），Agent Core 在中间件链中强制校验。

---

## 9. 记忆与知识库设计

### 9.1 记忆分层

```
┌─────────────────────────────────────┐
│  L1: 会话记忆（短期，默认 7 天）      │  ← 当前对话上下文，自动过期
├─────────────────────────────────────┤
│  L2: 个人记忆（长期，用户可管理）     │  ← 操作习惯、常用参数、偏好设置
├─────────────────────────────────────┤
│  L3: 组织记忆（科室/部门级）         │  ← 高频问法、报表模板、典型案例
├─────────────────────────────────────┤
│  L4: 集团知识库（全集团共享）         │  ← 制度文档、SOP、产品资料（Phase 3）
└─────────────────────────────────────┘
```

### 9.2 记忆边界（重要）

| 规则 | 说明 |
|------|------|
| **知情同意（PIPL）** | 首次使用时弹窗告知记忆机制及用途，员工**明确同意**后启用 L2 个人记忆；不同意则仅保留 L1 会话记忆（7 天自动过期），核心功能不受影响 |
| **可感知性** | 用户可以查看 Agent 记住了自己什么，可随时删除（个人记忆一键清除） |
| **默认隐私** | 个人记忆默认不跨科室共享 |
| **敏感隔离** | 涉及工资、合同金额、客户联系方式等敏感字段**不进入记忆层** |
| **遗忘机制** | L2/L3 记忆超过 90 天未被引用自动降权，180 天未引用归档 |
| **所有权** | 员工离职后，其个人记忆自动清除；组织记忆由科室管理员维护 |

### 9.3 记忆写入与检索机制

**写入触发规则（谁来写、何时写）：**

| 记忆层 | 写入触发 | 审核机制 |
|--------|----------|---------|
| L1 会话 | 每轮对话自动写入 | 无需审核，7 天 TTL |
| L2 个人 | 用户显式说"记住这个" + 高频行为自动沉淀（如同一报表连续 3 次查询） | 自动写入仅限偏好类（格式/默认参数）；写入后用户可在设置中查看和删除 |
| L3 组织 | 科室管理员/主管从 L2 或会话中**主动提炼**（如"这个问法很典型，存到科室知识库"） | 需科室管理员审核后生效 |
| L4 知识 | 信息科批量导入制度文档、SOP | 信息科管理员审核 |

**检索注入机制（何时用、怎么用）：**

- **意图识别阶段**：注入 L2（用户偏好）+ L3（科室高频问法）的 Top-K 相似记忆（向量检索，K=3），辅助意图消歧
- **参数提取阶段**：注入 L2 中的常用参数默认值（如"华东区"是用户常查的区域）
- **回复格式化阶段**：按 L2 偏好选择图表类型/报表模板
- **注入上限**：单次注入记忆 ≤ 500 Token，避免挤占业务上下文

### 9.4 记忆存储选型

- L1 会话记忆：Redis（7 天 TTL）
- L2/L3 记忆：向量数据库（Milvus / pgvector）+ 结构化元数据
- L4 知识库：RAG 向量索引 + 文档切片（见 9.5）

### 9.5 知识库（Knowledge Base）设计

> 参考 WorkBuddy 项目资产库（RAG 注入任务上下文）+ 腾讯乐享知识库模式。**记忆 vs 知识库的本质区别**：记忆是从用户交互中沉淀的行为数据（自动/半自动生成），知识库是人为上传管理的文档知识（制度、SOP、FAQ、案例）——两者共同构成 Agent 的"上下文供给"体系。

#### 9.5.1 知识空间分级

| 空间 | 管理者 | 内容 | 注入范围 | 阶段 |
|------|--------|------|---------|------|
| **集团知识库** | 信息科 | 制度文档、SOP、产品资料、FAQ | 全员（按文档密级过滤） | Phase 2 |
| **科室知识空间** | 科室管理员 | 科室 SOP、典型案例、审批口径、术语表 | 本科室 | Phase 2 |
| **个人知识空间** | 员工本人 | 个人笔记、常用资料 | 仅本人 | Phase 3 |

> 知识空间与记忆层 L3/L4 的关系：L3/L4 记忆存放"沉淀的行为经验"（问法/偏好/模板引用记录），知识空间存放"上传的文档知识"（文件本体 + 向量索引）——记忆引用知识（如"常用报表模板"指向知识库中的模板文档），不复制。

#### 9.5.2 知识来源与入库流程

```
来源① 文档上传：用户/管理员上传 Word/PDF/Excel/Markdown
来源② 系统对接：OA 制度库、乐享知识库（若集团已用）定时同步
来源③ 会话产物沉淀：任务产物（报告/分析结果）"保存到知识库"
来源④ L3 记忆升级：科室高频问法+答案固化 FAQ

入库流程：上传 → 自动解析与切片 →（涉密检测：D3/D4 级拦截或标记）
  → 管理员审核（集团/科室级） → 向量化入库 → 生效
```

**文档处理规则：**

| 环节 | 设计 |
|------|------|
| 解析 | 支持 docx/pdf/xlsx/md/pptx；扫描件走 OCR |
| 切片 | 按标题层级切片（优先章节结构），单块 300~500 Token，保留文档路径元数据 |
| 版本 | 文档更新 → 重新入库并替换向量；旧版本保留 90 天供溯源 |
| 溯源 | 每条回答引用知识时**必须附带来源文档名 + 章节**，杜绝"无出处的知识" |
| 失效 | 文档标记失效（制度废止）→ 向量立即下线；正在引用的会话给出"该制度已更新"提示 |

#### 9.5.3 知识注入机制（RAG）

**注入点设计（与 4.1 流水线对应）：**

| 流水线阶段 | 注入内容 | 说明 |
|-----------|---------|------|
| 意图识别 | 科室 FAQ/术语表（Top-3） | 消歧（如"请购单"在本科室的特殊叫法） |
| 参数提取 | 表单填写说明、制度中的字段规则 | 提升字段理解准确率 |
| 回复生成 | 制度条款、SOP 步骤（引用+原文摘录） | 回答"怎么报销""流程怎么走"类问题 |
| 技能执行 | 技能绑定的知识文档（如周报模板） | 自动化任务参数从知识库引用（见 3.6.2） |

**注入约束：**
- 单次注入 ≤ 800 Token（业务工具上下文优先于知识）
- 命中阈值：向量相似度 < 0.75 不注入，回复"知识库未找到，建议咨询 XX"
- **知识问答类意图**（"XX 制度怎么规定"）：走纯 RAG 问答流，不调用业务系统工具
- 敏感文档按 D 级数据分级过滤（10.2）：D3 脱敏展示、D4 不入 RAG

#### 9.5.4 知识库管理功能

| 功能 | 说明 |
|------|------|
| 知识浏览 | 按空间/目录/标签浏览，支持全文检索 |
| 上传与审核 | 上传 → 解析预览（自动展示切片效果）→ 提交审核 → 入库 |
| 使用统计 | 每篇文档的引用次数、命中率、最近引用时间（低引用知识定期提醒管理员更新或下线） |
| 知识缺口分析 | 统计"知识库未命中"的高频问题 → 推荐给对应科室管理员补充 FAQ |
| 权限管理 | 文档级权限（公开/指定科室/指定角色），与 2.3 权限矩阵联动 |

---

## 10. 安全与合规

### 10.1 关键动作的人在回路（Human-in-the-Loop）

以下动作 Agent **不得直接执行**，必须展示结构化确认卡并等待用户二次确认：

| 动作类别 | 示例 |
|----------|------|
| 审批类 | 同意/驳回/变更审批 |
| 财务类 | 提交付款单、修改凭证 |
| 库存类 | 触发出库、调整库存 |
| 人事类 | 修改考勤、调整薪酬 |
| 法务类 | 合同提交、用印申请 |

**确认卡要素：** 操作类型、影响对象、关键字段摘要、影响范围、执行后不可撤销说明、[确认] [取消] [修改] 按钮。

### 10.2 数据分级与安全措施

**数据分级标准（脱敏与权限的判定依据）：**

| 级别 | 定义 | 示例字段 | Agent 处理要求 |
|------|------|---------|---------------|
| D1 公开 | 可对全集团展示 | 产品目录、公告 | 无限制 |
| D2 内部 | 限集团员工 | 销售汇总数据、库存数量 | 登录即可见（按数据权限过滤） |
| D3 敏感 | 限授权岗位 | 客户联系方式、合同金额、供应商价格 | 展示脱敏（如手机号 138****5678）；导出加水印；不入记忆层 |
| D4 机密 | 限极少数岗位 | 薪酬数据、并购信息、核心配方 | MVP 不接入；接入后仅支持"有无/数量"类查询，禁止内容展示 |

**安全措施：**

| 措施 | 说明 |
|------|------|
| 传输加密 | 全站 HTTPS，系统间调用使用 mTLS |
| 敏感字段脱敏 | 按 D3 级定义在 API 返回和对话展示时自动脱敏 |
| 操作审计 | 每一次 Agent 触发的系统操作记录到审计日志，包含：用户、时间、动作、参数摘要、结果 |
| 防注入 | LLM 输出经 Schema 校验后才通过 MCP 调用，杜绝 Prompt 注入导致的越权调用 |
| 水印 | 导出的 Excel/PDF 自动添加用户信息水印 |

### 10.3 合规要求

- **本系统（Chat-Work-Agent）需通过等保 2.0 三级测评**（对接系统的安全水位由集团信息安全规范另行约束，不在本项目范围内）
- 审计日志留存 ≥ 180 天
- 支持法务科一键导出指定时间段的完整操作轨迹
- 记忆层涉及员工个人信息处理，须符合《个人信息保护法》要求（见 9.2 知情同意机制）

---

## 11. 非功能性需求

### 11.1 性能

| 场景 | 指标 |
|------|------|
| 简单查询响应 | ≤ 2s |
| 复杂统计（跨系统聚合） | ≤ 10s |
| 单据录入确认到提交 | ≤ 3s |
| 并发在线用户 | MVP 500 → Phase 2 2000 → Phase 3 5000 |

### 11.2 可用性

- 系统可用性 99.5%（工作日 8:00-20:00）
- 故障恢复时间 RTO ≤ 30 分钟
- 数据恢复点 RPO ≤ 5 分钟

### 11.3 可扩展性

- 新增业务系统只需实现对应的 **MCP Server**（封装为 4 种对接方式之一），不改 Agent Core
- 新增科室只需配置权限模板，不改代码
- LLM 模型可插拔（支持 OpenAI / 私有化部署 / 本地模型切换）

### 11.4 可观测性

| 维度 | 工具 |
|------|------|
| 业务指标 | Agent 完成率、用户满意度、高频意图 Top N |
| 系统指标 | 响应耗时、Token 消耗、MCP Server 成功率 |
| 安全事件 | 越权尝试、敏感操作、异常调用频率 |

---

## 12. 技术选型建议（草案，待评审）

> 详细技术架构规划（Monorepo 工程结构、模块设计、协议矩阵、部署架构、工程化规范）见 **[ARCHITECTURE.md](./ARCHITECTURE.md)**：技术基调 Python（服务端）+ TypeScript（客户端）+ Electron（桌面端，MVP 唯一客户端）。

| 层 | 建议方案 | 备选 |
|----|----------|------|
| **桌面端（MVP 唯一客户端）** | **Electron + React + electron-vite**（详见 5.5.2） | Tauri（包体小，Rust 栈） |
| 移动端（Phase 3） | 企业微信小程序 / Taro | 钉钉小程序 |
| **认证/身份层** | **Keycloak（OIDC Identity Broker，桥接 AD/企微/钉钉）** | 自研 Spring Authorization Server |
| Agent 后端 | Python + FastAPI + LangGraph | Node.js + NestJS |
| **MCP 协议层** | **官方 MCP SDK（Python）** + 自研 HTTP 传输适配 | **FastMCP**（社区轻量版） |
| **技能编排引擎** | LangGraph SubGraph（每个技能编排为一个子图，见 3.2.2） | 自研流程 DSL 解释器 |
| MCP Server 封装 | 每个系统独立 Python 进程，SSE 对外暴露 | 多系统合并一个 Server（MVP 可接受） |
| LLM | 企业级私有化模型 + 小模型路由 | 混合 OpenAI + 本地部署 |
| 向量库 | Milvus | pgvector |
| 关系库 | **MySQL 8.0+**（优先，兼容 PostgreSQL 16，见 ARCHITECTURE 4.6） | PostgreSQL |
| 缓存 | Redis | — |
| 网关层 | Kong / APISIX | 自研 Nginx |
| 部署 | Docker + K8s | Docker Compose（MVP） |
| 监控 | Prometheus + Grafana + 日志 | ELK |

---

## 13. 里程碑计划

| 阶段 | 时间 | 交付物 |
|------|------|--------|
| **MVP** | Month 1-3 | OA 单据录入 + BI 查询 + 销售科/采购科 + 审批待办 + 周报生成 + **桌面端客户端（Electron，Windows/统信）**；第 13 周小流量上线（10% 用户） |
| **Phase 2** | Month 4-6 | MVP 全量上线 + 新增 CRM/ERP/WMS + 信息/生产/计划/品管/仓库 + 技能市场 + 自动化任务 + 集团与科室知识库 + 跨系统编排 + **Web 管理后台（5.6：技能评审/知识库审核/自动化治理/系统配置工作台）** + 桌面端 macOS 版（视需求） |
| **Phase 3** | Month 7-12 | MES/U8 + 全部科室 + 个人/组织记忆 + 小程序 + 语音 |

### 13.1 MVP 开发进度（状态更新于 2026-09-18）

> 状态口径：✅ 已完成 = 代码交付 + 单测/桌面端 E2E 验证通过；⏳ 进行中；📋 待开发。

| 里程碑 | 状态 | 验证证据 |
|--------|------|----------|
| Agent Core 流水线（意图 → 技能路由 → 参数提取 → 校验 → HITL → 执行 → 格式化，含恢复图） | ✅ 已完成 | 单测 40 passed |
| `oa_leave_request` 请假录入（余额校验 / 字段提取 / 缺失补问 / 表单确认卡 / 幂等提交） | ✅ 已完成 | 桌面端 E2E 6 项通过 |
| `oa_todo_approve` 待办审批（待办列表 / 行内二次确认 / "这条"承接定位 / 驳回原因提取与通知） | ✅ 已完成 | 单测 + 桌面端 E2E 5 步通过（含 PRD 场景 2 核心验收句"这条驳回，原因是预算超了"全链路） |
| 审批流规则（< 3 天：直属主管 → HR 备案；≥ 3 天：直属主管 → **部门总监** → HR 备案） | ✅ 已验证 | 桌面端 E2E：3 天年假返回"审批流：直属主管 → 部门总监 → HR 备案" |
| `bi_query` / `weekly_report`（BI 查询 / 周报生成） | ✅ 已完成 | 单测 + E2E |
| 桌面端客户端（Electron）接入 Agent Core | ✅ 已完成（开发模式） | 桌面端 E2E 全链路（http://localhost:5174 → Agent Core 8011） |
| SSO 登录（8.5）：mock OIDC Broker（8012）/ 桌面端 Loopback 登录 + token 加密存储 + 静默刷新 / agent_core JWT 验签 / 前端登录门 | ✅ 已完成 | 桌面端单测 17 passed；agent_core 单测 54 passed；curl E2E 全链路（authorize→工号登录→code→token PKCE→/chat 带 Bearer 200）；SSO_REQUIRED=true 下无 token / 篡改 token 均 401 |
| 组织权限实接（P1.1，2.3 / 8.5.5）：流水线 permission 节点（validate 与 hitl 之间）——技能级写路径 required_roles any-of（oa_leave_request=employee / oa_todo_approve=dept_manager）；数据级业务维 BI 区域白名单；拒绝复用 validation.errors 渲染（不暴露权限细节） | ✅ 已完成 | agent_core 单测 87 passed（permissions 17 项）；E2E 14/14（services/agent_core/scripts/e2e_p1.py，SSO_REQUIRED=true）：员工行内审批拒绝 / dept_manager 放行 / BI 白名单外区域（华北）拒绝 |
| 审计日志（P1.2，10）：mcp_client 单点收口 tool_call 全量审计（perf_counter 计时 + contextvar 操作者注入）；auth_success / confirm_issued / confirm_approved / confirm_rejected / confirm_denied / permission_denied 埋点；内存 deque + Redis 双模式；GET /audit 审计流水查询 | ✅ 已完成 | E2E：E1001 审计动作全覆盖；数据级权限——员工仅本人、越权查询 403、dept_manager 全量；确认卡一次性（重复确认 404） |
| 生产部署 / 第 13 周小流量灰度（10% 用户） | 📋 待开发 | 依赖 LLM 部署方案与内网基础设施 |

---

## 14. 待确认问题清单

> 以下问题需与各方对齐后纳入下一轮迭代

- [ ] 各业务系统接口开放程度调研（是否有 OpenAPI？版本？鉴权方式？）
- [ ] OA 是否提供"按人查询操作日志"API（周报数据源，见场景 4 前置依赖）
- [ ] IM 集成方案（企微/钉钉群消息接口），决定周报"发送至群"能否进 MVP
- [ ] SSO 方案确认：推荐 Keycloak 作为 Identity Broker 收敛 AD/企微/钉钉（见 8.5），需确认集团是否已有统一认证平台可直接复用
- [ ] LLM 部署方案（私有化硬件资源？是否允许调用外部 API？数据出境合规？）
- [ ] **MCP 传输层适配**：MVP 用 SSE，Phase 2 是否切换到 Streamable HTTP？企业内网防火墙是否允许？
- [ ] **MCP Server 粒度**：MVP 每个系统独立一个 Server，还是多系统合并？需要评估运维复杂度
- [ ] **科室低代码技能搭建**（Phase 2 技能市场）：自研表单拖拽 + 流程编排工具，还是采购低代码平台？搭建门槛与审核流程需与信息科对齐
- [ ] **节假日日历数据源**（自动化任务"仅工作日"选项）：接入国务院节假日 API 还是 HR 系统维护？
- [ ] **知识库初始内容**：Phase 2 上线前需信息科梳理首批制度文档/SOP 并完成切片入库；集团是否已用乐享/Confluence 等知识平台可对接（见 9.5.2 来源②）？
- [ ] **自动化推送渠道**：企微机器人（群机器人 webhook）创建与管理规范由谁负责？
- [ ] **信创 OS 优先级**（桌面端）：统信 UOS / 银河麒麟是否 MVP 必须？影响 Electron 打包验证与测试机采购（见 5.5.1）
- [ ] **内网更新服务器与代码签名证书**（桌面端分发）：由信息科运维？Windows 企业代码签名证书申请周期较长，MVP 上线前必须就位，需提前启动（见 5.5.6）
- [ ] RPA 工具选型（UiPath / 影刀 / 自研？）
- [ ] 审计日志由谁负责存储与维护？法务科还是信息科？
- [ ] MVP 科室是否还需要财务科（单据涉及财务审批）？
- [ ] **多人协同会话权限模型**（Phase 2）：会话内各人权限不同时，Agent 以"发起人"还是"最严格权限"执行？
- [ ] 是否需要离线降级方案（LLM 不可用时，Agent 转为固定指令集模式）？

---

## 15. 风险与应对

| # | 风险 | 影响 | 概率 | 应对策略 |
|---|------|------|------|---------|
| R1 | **接口不开放**：OA/BI 实际无可用 OpenAPI | MVP 集成层返工 | 中 | Week 1-2 完成接口调研作为前置关卡；不开放则降级为 RPA/JDBC 只读，场景范围同步缩减 |
| R2 | **重复录单**：写入超时后重试或用户重复提交 | 财务/库存数据错误 | 低 | 幂等键机制（见 8.7）+ 确认卡防抖（提交后按钮置灰） |
| R3 | **LLM 幻觉**：参数编造、枚举值不存在 | 错误单据进入业务系统 | 中 | Schema 强校验 + 枚举值对齐 + 测试集回归（16.2 参数值准确率 ≥ 98% 门槛） |
| R4 | **用户接受度低**：员工不信任 AI 录单 | 日活不达标 | 中 | 灰度先行（5 人 → 10% → 全量）；确认卡全透明；上线首月配专人答疑 |
| R5 | **权限模型复杂**：区域+组织双维度配置错误 | 数据越权泄露 | 低 | 安全拦截率 100% 红线（16.2）；权限配置由系统管理员双人复核 |
| R6 | **Token 成本超预期**：实际用量为估算 2-3 倍（含自动化任务无人值守消耗） | 预算超支 | 中 | 热点查询缓存（相同 BI 查询 5 分钟内复用）；小模型路由（意图识别用小模型）；自动化任务并发与频率限额（见 3.6.2） |
| R7 | **SSO/HR 主数据质量差**：组织归属错误 | 权限映射失效 | 中 | 上线前 HR 数据清洗；登录时显示归属科室让用户确认 |
| R8 | **进度延期**：依赖方（OA 厂商）配合慢 | MVP 里程碑失守 | 高 | 里程碑中预留 2 周缓冲；厂商接口联调提前至 Month 1 启动 |
| R9 | **自动化任务异常无人发现**：推送失败/结果错误持续多日 | 决策依据失真 | 中 | 连续 3 次失败自动暂停并通知创建者（3.6.2）；执行历史全量记录；推送链路告警接入运维监控 |
| R10 | **知识库内容过期/错误**：制度已更新但向量未同步 | 错误答复误导员工 | 中 | 文档版本管理与失效下线机制（9.5.2）；回答强制附来源可溯源；低引用知识定期提醒更新 |
| R11 | **桌面端分发安全**：安装包被篡改/更新劫持/本地数据泄露 | 终端失陷、集团内网横向渗透 | 中 | 代码签名 + 更新包哈希校验（5.5.3）；contextIsolation + IPC 白名单；token 走 OS 密钥链；本地缓存 AES 加密；强制升级通道 |
| R12 | **桌面端维护成本**：Electron 版本跟进、多 OS 打包测试、信创适配消耗人力 | MVP 进度挤占 | 中 | 先 Windows + 统信两平台（macOS 延后至 Phase 2）；单一客户端形态（无 Web 双轨维护）；打包流水线自动化 |

---

## 16. 评估方法论

> Agent 质量必须通过可量化的测试集验证，不能"感觉差不多"。

### 16.1 测试集构建

MVP 阶段为每个场景收集 **50+ 条真实用户意图**，覆盖正常路径、边界条件、错误输入：

| 场景 | 测试集来源 | 典型样本 |
|------|-----------|---------|
| 请假审批 | HR 招募 5 名跨科室员工 | 年假/调休/病假、跨周时长计算、余额不足、缺事由 |
| BI 查询 | 提前设计高频问法 | "本月华东销售"、"Q3 应收账龄"、"按产品拆分" |
| 审批 | 提前设计 | 同意、驳回、批量处理、无权限操作 |
| 报销审批（Phase 2，见 6.1.2） | 财务科 3 位同事，近 2 周真实报销单 | 多行明细、金额自动求和、发票张数追问、大额审批流 |
| 请购单录入（Phase 2，见 6.1.2） | 采购科 3 位同事，近 2 周真实请购单 | 办公耗材、生产物料、预算边界值 |
| 销售订单录入（Phase 2，CRM 对接后） | 销售科 3 位同事，近 2 周真实订单 | 标准订单、样品单、多行商品、客户名简写、缺字段 |

### 16.2 评估维度与通过标准

| 维度 | 检查内容 | MVP 通过标准 |
|------|----------|-------------|
| **意图识别准确率** | 用户输入 → 正确意图 | ≥ 90% |
| **技能路由命中率** | 用户问法命中正确技能（含触发问法变体） | ≥ 85%（命中技能后走编排，准确率显著高于裸路由） |
| **工具选择准确率** | 选对系统 + 选对 Tool | ≥ 95% |
| **参数提取完整率** | 必填字段无遗漏 | ≥ 92%（缺失后能自动追问算通过） |
| **参数值准确率** | 金额、日期、关联 ID 正确 | ≥ 98% |
| **确认卡可接受率** | 用户直接点"确认"的比例 | ≥ 70%（剩余 30% 是正常修改字段） |
| **写入成功率** | 确认后实际写入目标系统成功 | ≥ 99% |
| **查询结果一致率** | Agent 返回值与在 BI/OA 手动查询一致 | 100% |
| **安全拦截率** | 越权尝试被成功拦截 | 100%（不得有漏网） |
| **平均响应时间** | 从发送到首字显示 | ≤ 2s |

### 16.3 评估流程（对齐 MVP 3 个月周期）

```
Week 1-9:   开发（含每周 50 条样本的滚动自测）
Week 10:    内部工程师自测（Dev QA）
Week 11:    测试集全量跑一遍 → 记录失败 case → 修复 → 重跑
Week 12:    邀请 5 位销售科/采购科真实员工灰度测试 → 收集反馈 + 修复
Week 13:    生产部署，小流量上线（10% 用户）
Phase 2 初期: 观察灰度指标 → 迭代 → 全量上线（与 Phase 2 开发并行）
```

### 16.4 灰度期间观测指标（每日）

- Agent 对话总数 / 活跃用户数
- 确认卡"修改字段"占比（太高说明参数提取不准）
- 写入失败次数 + 失败原因分布
- 用户手动退出会话的比例

---

## 17. 成本估算（MVP）

> 以下为 MVP 阶段（3 个月）的预估，基于 LLM 私有化部署方案。

### 17.1 硬件资源

| 资源 | 规格 | 数量 | 用途 | 月租金（参考） |
|------|------|------|------|---------------|
| GPU 服务器 | A10 × 2 卡 | 1 台 | 私有化 LLM 推理 | ¥15,000 ~ ¥25,000 |
| 应用服务器 | 8C 32G | 3 台（Agent + Gateway + 桌面更新服务器） | 业务服务 | ¥3,000 ~ ¥5,000 |
| 数据库服务器 | 8C 32G + SSD | 2 台（主从） | MySQL 8 + Redis | ¥3,000 ~ ¥5,000 |
| 向量数据库 | 8C 16G | 1 台 | Milvus（MVP 可与 MySQL 共用） | ¥0 ~ ¥2,000 |
| 认证服务 | 2C 8G | 2 台（主备） | Keycloak（Identity Broker，见 8.5） | ¥1,000 ~ ¥2,000 |

**硬件月合计：¥22,000 ~ ¥39,000**

### 17.2 Token 消耗估算（如果不私有化）

> 如果选择云服务方案（如通义千问 / 文心一言 / OpenAI），按 MVP 估算：

| 指标 | 估算值 | 说明 |
|------|--------|------|
| 日均对话数 | 1,500 | MVP 500 用户 × 3 条/人/天 |
| 平均输入 Token | 800 | 含会话历史 + 系统 prompt + 工具描述 |
| 平均输出 Token | 300 | 含参数 JSON + 自然语言回复 |
| 日均消耗 | ~165 万 Token | (800+300) × 1,500 |
| 月消耗 | ~4,950 万 Token | |
| 预估费用 | ¥5,000 ~ ¥15,000/月 | 取决于模型价格档位 |

> **Phase 2 自动化任务增量**（预警）：假设 30% 用户创建 1~2 个自动化任务（日跑 1 次，单次 2,000~5,000 Token），日增消耗 30~50 万 Token（+20%~30%）。已通过 R6 风险的并发/频率限额与热点缓存控制（见 15 章）。

### 17.3 人力投入

| 角色 | 人数 | 周期 | 主要工作 |
|------|------|------|---------|
| 产品经理 | 1 | 全程 | 需求细化、优先级排序、用户调研 |
| Agent 后端 | 2 | Month 1-3 | Agent Core、MCP Client、中间件层 |
| 前端（Electron） | 1 | Month 1.5-3 | 桌面端主进程/preload/renderer、Chat UI、确认卡、卡片组件、自动更新 |
| 集成工程师 | 1 | Month 1.5-3 | OA/BI MCP Server 封装、SSO |
| 测试 | 1 | Month 2.5-3 | 测试集、自动化、灰度 |
| UI 设计 | 0.5 | Month 1-2 | Chat UI 原型、确认卡样式 |

### 17.4 成本结论

**私有化部署方案（MVP 月均基础设施 ¥22,000 ~ ¥39,000，即 17.1 合计）**
- GPU 推理 ¥15,000~25,000 + 应用/数据库/向量/认证 ¥7,000~14,000
- 首月另计一次性费用：模型部署调优、MCP Server 开发环境搭建
- 人力投入见 17.3（不计入月度基础设施）

**云 API 方案（MVP 月均 ¥12,000 ~ ¥29,000）**
- 基础设施 ¥7,000~14,000（同 17.1 去掉 GPU）+ Token 费 ¥5,000~15,000（17.2）
- 前提：集团允许业务数据调用外部 LLM API，需通过数据出境合规评估

**扩展成本（与 17.1 口径一致）**：500 并发 ≈ 2 张 A10，即**每新增 500 并发约需增加 2 张 A10 GPU**（约 ¥6,000~10,000/月）

> ⚠️ 以上为粗略估算，具体需在技术选型确认后做精确 TCO 分析。

---

## 18. 附录：术语表

| 术语 | 全称 | 说明 |
|------|------|------|
| OA | Office Automation | 办公自动化系统（审批流、公文） |
| BI | Business Intelligence | 商业智能/报表分析系统 |
| CRM | Customer Relationship Management | 客户关系管理（集团内为销售下单/跟单系统） |
| ERP | Enterprise Resource Planning | 企业资源计划 |
| WMS | Warehouse Management System | 仓库管理系统 |
| MES | Manufacturing Execution System | 制造执行系统 |
| U8 | — | 用友 U8 财务/ERP 软件 |
| MCP | Model Context Protocol | LLM 工具调用标准协议，定义工具发现/调用规范 |
| Skill / 技能 | — | 面向业务场景的能力扩展包：编排多个 MCP 工具 + 表单 + 确认（见第 3 章，参考 WorkBuddy SkillHub） |
| Skill Store / 技能市场 | — | 技能浏览、安装、上架审核、使用统计的交换平台 |
| Automation / 自动化任务 | — | 技能的无人值守执行形态：调度规则 + 结果推送（见 3.6.2，参考 WorkBuddy 自动化） |
| Knowledge Base / 知识库 | — | 人为上传管理的文档知识（制度/SOP/FAQ），RAG 注入 Agent 上下文（见 9.5） |
| Electron | — | 跨平台桌面应用框架（Chromium + Node.js），MVP 唯一客户端技术（见 5.5） |
| Tauri | — | 轻量桌面框架备选（系统 WebView + Rust 后端） |
| Loopback 回调 | — | 桌面端 OAuth 授权最佳实践：`127.0.0.1:随机端口` 接收回调 code（RFC 8252） |
| 信创 | 信息技术应用创新 | 国产化软硬件替代（统信 UOS、银河麒麟 OS 等） |
| HITL | Human-in-the-Loop | 人在回路：关键操作需人工二次确认 |
| RAG | Retrieval-Augmented Generation | 检索增强生成 |
| SSO | Single Sign-On | 单点登录 |
| NPS | Net Promoter Score | 净推荐值（用户满意度指标） |
| RTO / RPO | Recovery Time / Point Objective | 故障恢复时间目标 / 数据恢复点目标 |
| TCO | Total Cost of Ownership | 总拥有成本 |
| PIPL | Personal Information Protection Law | 《个人信息保护法》 |
| 等保 2.0 | — | 网络安全等级保护制度 2.0 标准 |
| cron | — | 定时任务表达式语法（技能定时触发用） |

---

*— End of PRD —*
