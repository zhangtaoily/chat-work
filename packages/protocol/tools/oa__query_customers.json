{
  "name": "oa__query_customers",
  "description": "按客户名称模糊查询 OA 客户档案（只读：录入单据前的客户枚举对齐）",
  "inputSchema": {
    "type": "object",
    "properties": {
      "customer_name": {
        "type": "string",
        "title": "客户名称",
        "description": "客户名称关键词（模糊匹配）"
      }
    },
    "required": ["customer_name"]
  },
  "annotations": {
    "title": "查询客户档案",
    "readOnlyHint": true,
    "_meta": {
      "humanConfirmation": "none",
      "idempotencyRequired": false,
      "rw": "read",
      "requiredPermissions": ["oa:customer:read"]
    }
  }
}
